# Shared surfaces — registered behaviors

Fork behaviors whose code lives in files **upstream also owns**. That is the whole
reason this file is separate: a three-way merge can only take upstream's version of a
region silently when upstream has a version of that file. Fork-only files cannot be
reverted without a conflict; these can.

**During a sync, read this file first.** When a conflict lands in `src/app/state.rs`,
`src/ui.rs`, `src/app/input/sidebar.rs`, `src/client/mod.rs` or `src/ui/panes.rs`, the
"Breaks if lost" column is what decides whether a resolution is acceptable.

Four families live in their own files but sit in upstream-owned code all the same —
consult them under the same rule during a sync:
[`tab-attention.md`](tab-attention.md) (`workspace.rs`, `workspace/tab.rs`,
`ui/tabs.rs`, `api/tabs.rs`, `api/plugins/panes.rs`, `persist/restore.rs`,
`app/runtime.rs`), [`agent-panel.md`](agent-panel.md) (`ui/sidebar.rs`),
[`workspace-chats.md`](workspace-chats.md) (`app/session.rs`, plus the fork-only
`persist/workspace_chats.rs`) and [`surface-chrome.md`](surface-chrome.md)
(`ui/sidebar.rs`, `ui/widgets.rs`, `ui.rs`, `app/input/sidebar.rs`,
`ui/shell/model.rs`, `ui/shell/layout.rs`, `ui/shell/view.rs`,
`config/model.rs`, `config/io.rs`, plus the fork-only `ui/shell/source.rs`).

`surface-chrome.md` is the one to watch hardest in `ui/widgets.rs`: the chip and
bar shells sit beside upstream's own popup and modal helpers in that file, and a
merge that takes upstream's version of the module drops them without a conflict
anywhere. Its geometry also reaches into `app/input/sidebar.rs`, where drawing
and hit testing have to keep reading the same chrome — a resolution that
restores a constant there compiles, passes, and silently moves every row in the
left panel one cell off what the person can see.

The bar-section half of that family reaches deeper into upstream's own shell
module, and each of those three files loses something different if a merge
takes upstream's side. `ui/shell/layout.rs` holds `allocate_tracks`, the
extracted three-phase core: restoring upstream's inlined version compiles
cleanly and leaves the section allocator behind, at which point sections are
divided by nothing at all. `ui/shell/view.rs` holds the `BarSection` hit target
and the pass that produces it from the key's own bars — losing it leaves the
sections drawn but unclickable, which reads as an input bug rather than a lost
merge. `ui/shell/model.rs` holds `Hash` on `TrackPolicy` and the placement
reachability check; without the first, the sections cannot live in the geometry
key at all.

Format and rules: [`README.md`](README.md).

## Machine gate: per-display scheduling

One failure in this family cannot be pinned by a row, because it is not a
behavior in one place — it is a *pairing*. A field is per-display and its
consumer is correct, but the consumer is scheduled outside every display's
view. Both halves compile, every unit test passes, and once a second display
attaches the consumer reads nobody's state: the request is taken, nothing
matches, and the action is dropped in silence on every display.

`scripts/test_per_display_scheduling.py` (run by `just check`) enforces the
pairing directly against the source: no scheduled task may read a per-display
field from outside `for_each_display`, and the monolithic and headless
schedulers must drive the same per-display work. Deliberate exceptions are
listed in `OUTSIDE_LOOP_ALLOWED` with their reason.

## Activation and keybinding

How the fork's surfaces are reached at all. Upstream owns the keybind registry, the navigate-mode dispatcher and the help screen, so these are the first rows a sync can quietly unbind.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-ACT-1 | `toggle_file_manager` opens the file manager and closes it again, symmetrically. | The toggle becomes one-way: the surface opens but the same action no longer dismisses it. | `toggle_file_manager_opens_and_closes` |
| TP-ACT-2 | Opening the file manager starts from the active workspace's directory, not a process-global cwd. | The file manager opens somewhere the user was not working, in the wrong workspace. | `open_file_manager_uses_active_workspace_cwd` |
| TP-ACT-3 | The bound key (default prefix+f) drives the toggle end-to-end through the navigate-key handler: enum, mapping and dispatch wiring together. | The action exists but no keystroke reaches it, because one link in the enum/mapping/dispatch chain was dropped. | `prefix_f_key_toggles_file_manager` |
| TP-ACT-4 | Dispatching the `ToggleFileManager` action toggles the surface *and* leaves navigate mode. | The user lands in the file manager with navigate mode still armed, so the next keystroke is interpreted by the wrong owner. | `toggle_file_manager_action_opens_and_leaves_navigate_mode` |
| TP-ACT-5 | The file manager action is discoverable in the keybind help. | The feature becomes undiscoverable: it works but nothing tells the user it exists. | `help_lists_the_file_manager_action` |
| TP-ACT-DEFAULT | The shipped default keybinds bind prefix+f to the file manager. | A fresh install has no way to open the fork's main surface without hand-editing config. | `default_binds_prefix_f_to_file_manager` |
| TP-M1.1-KEYBIND | The agent-attachment picker owns exactly one configurable prefix action and registers it through the existing user-first conflict registry rather than hard-coding input. | A user's own binding is silently overridden, or the fork's binding collides with an upstream one and one of them stops working. | `default_binds_prefix_a_to_agent_attachment_picker_without_conflict` |

## Repaint gate

The router decides which input events are worth a redraw. This lives in upstream's mouse routing, and the failure mode is not a wrong pixel but a terminal that repaints on every mouse move.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-REPAINT-2B | A hover move with no blocking overlay (plain terminal or the native file-manager surface) changes nothing herdr draws, so the router must not request a render for it. | Every pointer movement across the window forces a full repaint, wasting CPU and flickering on slow links. | `inert_mouse_move_declines_render` |
| TP-REPAINT-2C | Generic press, release and drag input always requests a repaint. | A click produces no visible response until some unrelated event happens to redraw. | `non_move_mouse_events_always_request_render` |
| TP-REPAINT-2D | While a hover-sensitive overlay owns the pointer, a move can change its highlight, so the router keeps requesting a render. | Menu and popup highlights stop following the mouse, because the move gate suppressed the repaint they needed. | `mouse_move_over_blocking_overlay_requests_render` |
| TP-REPAINT-2E | Non-mouse interaction (keys, paste) is low-frequency and always repaints, unchanged by the move gate. | A repaint optimisation aimed at mouse traffic accidentally swallows keyboard feedback. | `keyboard_and_paste_always_request_render` |
| TP-REPAINT-2F | Wheel input repaints on the same generic path as press/release/drag; the native-FM vertical wheel has its own exact typed override. | Scrolling produces no visible movement until another event triggers a draw. | `non_move_mouse_events_always_request_render` |

## compute_view composition

`compute_view` is upstream's, and the fork hangs a great deal off it. Every row here is a rule about geometry published for one frame and retired with it.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-A3.2-VIEWPORT | `compute_view` owns viewport normalization for both responsive layouts: shrinking or expanding the available height keeps the cursor visible and clamps stale offsets to the new maximum. | After a resize the cursor is scrolled off-screen, or a stale offset points past the end of the list. | `compute_view_normalizes_file_manager_viewport_after_resize`, `current_row_actions_follow_miller_geometry_at_all_breakpoints`, `enter_and_leave_normalize_viewport_for_new_directory`, `viewport_follows_cursor_and_clamps_at_both_edges`, `viewport_handles_zero_rows_reload_shrink_and_empty_list` |
| TP-C2.1-VIEWSTATE | Desktop `compute_view` snapshots the current name and action rects from one geometry source, then clears both when the file manager closes. | Stale terminal coordinates stay clickable after the surface is gone, so a click hits an invisible target. | `compute_view_snapshots_and_clears_file_manager_row_areas` |
| TP-N3.1-LIFECYCLE | `compute_view` rebuilds persistent action-bar content after navigation or reload, clears it on close, and restores the current empty selection plus the client-local clipboard summary on reopen. | The action bar shows the previous directory's state, or the clipboard summary vanishes on reopen. | `compute_view_refreshes_and_clears_file_manager_action_bar_content` |
| TP-C6.4-VISUAL | Expanded, collapsed desktop and responsive mobile layouts compose the same exact state without stale sidebar or row authority, and overlays paint above the composed surface without changing its prepared operation state. | A layout breakpoint shows a different truth than the state holds, or opening a menu mutates the pending operation. | `native_fm_composes_sidebar_breakpoints_and_status_across_full_frames`, `native_fm_context_and_delete_modal_compose_above_status_surface` |
| TP-FMN-NAV-08 | The legacy Miller projection runs first during `compute_view`, but its viewport may not drag an inactive preview child into focus before the Trail projection is published. | Focus lands on a preview column the user never activated, so the next keystroke acts on the wrong directory. | `compute_view_auto_follow_tracks_active_trail_owner` |

## Context menu, plugins and destructive confirmation

These converge on upstream's popup, menu-render and dialog owners. The recurring rule is that authority is typed and prepared in advance — never reconstructed from what the popup happens to be showing.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-C3.1-CONTEXT-MODEL | Cursor focus or an empty prepared selection does not invent a menu; the six core actions keep deterministic order; read-only state disables only cwd-writing actions; multiple selection permits only bulk-capable actions while preserving prepared path order; and unsupported, stale or in-flight authority disables every item, with in-flight taking priority. | A menu appears for nothing, actions reorder under the user's hand, or an action runs against a selection that is stale or already being operated on. | `file_context_kind_exposes_deterministic_labels`, `file_context_menu_requires_explicit_prepared_selection`, `invalid_or_in_flight_file_context_menu_fails_closed`, `multiple_file_context_menu_disables_single_target_actions`, `single_file_context_menu_has_stable_order_and_read_only_authority` |
| TP-C3.2-POPUP-LIFECYCLE | Disabled file actions stay visibly dim even when highlighted, enabled rows keep normal and selected contrast, and the model is read-only during rendering. | A disabled action looks enabled once highlighted, inviting a click that silently does nothing — or rendering mutates the menu it is drawing. | `disabled_and_stale_file_context_actions_fail_closed`, `disabled_file_context_items_have_distinct_highlight_safe_style`, `file_context_menu_keyboard_owns_focus_and_emits_exact_intent`, `file_context_menu_mouse_hover_click_outside_and_close_lifecycle` |
| TP-C3.3-PLUGIN-SURFACE | Plugin actions append after built-ins in stable qualified-id order, carry the exact prepared paths through the neutral public plugin API and back across JSON, size the shared popup by display cells rather than byte length, and fail closed on unknown action contexts. Lossy path conversion is forbidden: a Unix path JSON cannot represent exactly keeps built-ins but exposes no plugin action. | A plugin receives the wrong file, a non-UTF-8 path silently becomes a different path, unicode titles corrupt the popup frame, or link order changes the menu. | `file_context_menu_appends_plugins_and_serializes_exact_path_intent`, `file_context_menu_hides_plugins_for_non_utf8_paths`, `file_context_menu_plugin_action_is_typed_and_disable_race_fails_closed`, `file_manifest_actions_are_enabled_filtered_and_deterministic`, `manifest_rejects_unknown_file_action_context`, `plugin_context_merge_preserves_exact_file_paths`, `plugin_file_action_context_round_trips_exact_paths`, `plugin_file_context_menu_uses_display_width_for_unicode_title` |
| TP-C4.2-CONFIRM | The first modal makes reversible and irreversible choices explicit and reports the exact bounded selection size; irreversible authority has a visibly separate second gate that cannot be mistaken for the default trash action. | A permanent delete is one keystroke away from a trash delete, or the user cannot tell how many files they are about to destroy. | `context_delete_opens_the_same_confirmation_model`, `delete_confirmation_ignores_modified_destructive_shortcuts`, `delete_confirmation_mouse_buttons_are_bounded_and_fail_closed`, `delete_confirmation_rejects_empty_and_inflight_authority`, `file_delete_choose_action_renders_distinct_safe_choices`, `file_delete_permanent_stage_renders_irreversible_warning`, `header_delete_opens_exact_confirmation_without_mutation`, `permanent_delete_requires_second_confirmation`, `stale_or_reopened_confirmation_cannot_emit_delete_request`, `trash_confirmation_emits_request_while_cancel_is_side_effect_free` |
| TP-C6.3-AUTHORITY | A revalidated plugin intent reaches the existing App-owned command runtime exactly once; the scheduler consumes the typed intent and never infers an action from the popup title. | An action runs twice, or a renamed menu label changes which command executes. | `context_open_converges_on_existing_navigation_authority_once`, `file_manager_plugin_intent_uses_existing_command_runtime_once`, `row_delete_converges_on_shared_typed_confirmation_authority`, `compress_context_action_writes_the_archive_beside_its_sources` |
| TP-C6.3-LIFECYCLE | Closing the surface invalidates every pending action authority immediately, before a same-cwd reopen could make stale paths look current again at the scheduled boundary. | An action prepared before close executes after reopen against paths that only look the same. | `close_file_manager_clears_pending_action_authority`, `compress_context_action_writes_the_archive_beside_its_sources` |

## Locations in the shell sidebar

The Locations rail is fork work living inside upstream's sidebar. Ownership is the theme: Files is a center-stage launcher and must not take over global shell surfaces.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-C6.1-MODEL | Filesystem discovery happens before render: existing well-known directories keep Finder order, missing favorites are omitted, configured pins stay visible but marked inaccessible, duplicate path authority stays with the first section, optional PINNED disappears when empty, and adversarial configuration cannot create an unbounded model. | Render blocks on the filesystem, a hostile config grows the sidebar without limit, or a duplicated path makes a later row steal authority from an earlier one. | `file_locations_model_is_bounded_across_all_sections`, `file_locations_model_orders_sections_and_deduplicates_path_authority`, `file_locations_preparation_uses_live_home_and_pin_state` |
| TP-C6.1-LIFECYCLE | Location preparation reads live home and pin state at preparation time rather than trusting a cached snapshot. | The rail shows the pins or home directory from an earlier moment. | `file_locations_preparation_uses_live_home_and_pin_state`, `location_navigation_opens_exact_directory_and_rejects_stale_targets` |
| TP-C6.1-NAV | A content rail row carries exact path identity; mouse input prepares one typed request and performs no directory read itself. | Clicking a location reads the filesystem on the input path, or navigates by row index instead of by path. | `clicking_file_locations_rail_item_prepares_exact_typed_navigation_request`, `location_navigation_opens_exact_directory_and_rejects_stale_targets`, `stale_file_locations_rail_hit_area_is_inert_after_model_refresh` |
| TP-C6.1-GEOMETRY | Cached hit geometry cannot authorize a path after the prepared model changes underneath it. | A click resolves against a rail that has already been rebuilt, opening whatever now sits at those coordinates. | `stale_file_locations_rail_hit_area_is_inert_after_model_refresh` |
| TP-FCL-INPUT-01 | A fresh row click and vertical rail scroll are content-owned, and raw host bytes at the remote-client boundary prepare the exact typed request. | Rail interaction is claimed by the global sidebar, or a remote client's clicks do not reach the same request path a local one does. | `clicking_file_locations_rail_item_prepares_exact_typed_navigation_request`, `fcl_input_fresh_row_click_and_vertical_rail_scroll_are_content_owned`, `headless_raw_mouse_locations_navigation_loads_exact_trail` |
| TP-FCL-SHELL-01 | Files is a center-stage launcher: the global Spaces projection, including its workspace/agent tracking body and its wheel, stays owned by Spaces after activation — even when a legacy Files tab value is present. | Opening Files hijacks the global workspace tracker, so the user loses the sidebar view they were using. | `fcl_shell_files_activation_preserves_spaces_sidebar_projection`, `legacy_files_tab_value_keeps_visible_spaces_wheel_interaction`, `legacy_files_tab_value_renders_spaces_tracker_not_locations` |
| TP-FCL-SHELL-02 | Projects and Files are independent presentation owners; opening Files cannot silently switch the global body away from a user-selected Projects view. | Opening a file browser silently changes an unrelated part of the shell. | `fcl_shell_files_activation_preserves_projects_sidebar_owner` |
| TP-FMR-SIDEBAR-HL-01 | Raw host mouse bytes arriving at the headless server prepare the exact typed locations request. The source pins -01, -02 and -03 together with one live headless test, because the App-level test cannot exercise the real remote boundary. | Remote clients get different navigation behavior from local ones, and no App-level test can see it. | `headless_raw_mouse_locations_navigation_loads_exact_trail` |
| TP-FMR-SIDEBAR-HL-02 | The headless scheduled loop consumes the prepared request into the existing Files generation rather than creating a second authority. Pinned together with -01 and -03. | A remote-driven navigation lands in a generation nothing else is reading. | `headless_raw_mouse_locations_navigation_loads_exact_trail` |
| TP-FMR-SIDEBAR-HL-03 | The resulting Trail is the exact requested one, verified across the real client boundary rather than in-process. Pinned together with -01 and -02. | The remote path resolves to a near-miss directory and only a live client would reveal it. | `headless_raw_mouse_locations_navigation_loads_exact_trail` |

## Image preview in server mode

The file manager's image preview is driven by a bounded worker that only advances
when something drives it, and it decodes against a cell size only the client knows.
Both halves live in the server loop, and both were missing — so a preview that
worked perfectly in the local TUI showed nothing at all over a socket. These rows
exist because that failure was completely silent: no error, no warning, just a
panel that stayed empty.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-FMR-IMAGE-HL-01 | The headless scheduled loop drives the image preview worker, exactly as the monolithic loop does. | Server-mode image previews never leave `Pending`: the decode is never started, so the panel stays empty forever with nothing reported. | `headless_scheduler_syncs_the_image_preview_worker` |
| TP-FMR-IMAGE-HL-02 | The foreground client's resolved cell size is published to the image preview from the same decision that resolves host graphics, not from a second policy. | The preview decodes against a cell size of zero, derives no target and renders nothing — a second silent blocker that survives even after the scheduler call is restored. | `headless_publishes_foreground_cell_size_to_the_image_preview` |
| TP-FMR-IMAGE-HL-03 | A ready file-manager image reaches the client as kitty graphics inside the streamed frame. | The decode succeeds but the pixels never cross the socket, so every earlier link can be green while the user still sees nothing. | `server_frame_carries_fm_image_graphics_when_ready` |
| TP-GFX-ZLIB-01 | Kitty graphics payloads at or above 64 KB travel deflated: one zlib stream over the whole payload before chunking, announced with `o=z` on the first chunk only, with the geometry keys untouched. `experimental.kitty_graphics_compression = false` restores the uncompressed bytes exactly. | A remote client pays raw RGBA for every frame — one measured browser click cost 25 MB where the same pixels deflate 147x in 3 ms — and a payload compressed per chunk instead of once decodes as garbage in the terminal. | `large_payload_is_compressed_and_inflates_to_the_original`, `compression_covers_the_whole_payload_before_chunking`, `small_payload_is_left_uncompressed`, `disabled_compression_is_byte_for_byte_the_old_output`, `geometry_keys_survive_compression` |
| TP-FMR-IMAGE-HL-04 | A client whose cell size is unknown receives no graphics and no panic; the size is never guessed. | A guessed cell size is worse than none: kitty scales to exactly fill the given cell box, so the image is silently stretched instead of simply absent. | `unknown_cell_size_client_gets_no_graphics_and_no_panic` |
| TP-SRV-SCHED-PARITY-01 | The headless and monolithic schedulers make the same set of `sync_*`/`refresh_*` calls, apart from an explicit in-source difference list; closing a gap forces that list to be updated. | The two loops drift apart again and a feature works locally but not over a socket, with no test failing until a human notices. | `scheduler_parity_headless_vs_monolithic` |
| TP-SRV-HANDOFF-DIAG-01 | A live-handoff wait that runs out of time reports which wait it was and the budget it was given. | Every read in the handoff protocol carries `SO_RCVTIMEO`, so an exhausted wait surfaces as a bare `EAGAIN`: the person running `herdr update` on a busy machine is told only "Resource temporarily unavailable (os error 11)", with no step and no budget. That message cost three sessions to diagnose once. | `a_timed_out_handoff_wait_names_its_step_and_budget` |
| TP-SRV-HANDOFF-DIAG-02 | Only a timeout is renamed; a token mismatch, a closed stream or a version refusal passes through untouched. | Wrapping every failure in waiting language describes the wrong failure, and a permission or version problem starts reading as a slow machine. | `a_handoff_failure_that_is_not_a_timeout_is_left_alone` |
| TP-FMR-IMAGE-HL-05 | Leaving the Files surface and returning places the image again. | The preview survives only until the first tab switch, which is barely a preview. | `fm_image_graphics_return_after_leaving_and_reentering_the_files_surface` |
| TP-FMR-IMAGE-HL-06 | An overlay anchored over the preview — the context menu, a confirmation — leaves the image on screen. | Right-clicking a row deletes the picture the menu was opened on top of, and it does not come back; the preview panel stays empty for the rest of the session, with nothing reported. | `fm_image_survives_a_context_menu_opening_over_it` |
| TP-FMR-IMAGE-HL-07 | An overlay that covers the whole frame takes the image with it. | A terminal image is not a cell, so text drawn over it leaves the picture hanging on top of an unrelated full-screen page — a worse failure than a missing preview, and the exact way an over-eager fix to -06 would break. | `full_screen_overlay_takes_the_fm_image_away` |
| TP-FMR-IMAGE-HL-08 | Modes that cover nothing — prefix, navigate — keep the image, because the gate is a question about the surface rather than about the mode. | The preview blinks out whenever a transient mode is entered: the same defect as -06 wearing different clothes, which is why the rule is a classification and not a special case for menus. | `transient_modes_that_cover_nothing_keep_the_fm_image` |
| TP-FMR-IMAGE-HL-09 | Graphics authority follows stage ownership, not "is the file manager open somewhere": an open but backgrounded Files tab does not claim the placement pass. | A Files tab nobody is looking at places its preview over the terminal that is on screen, and the visible panes lose their own images. | `backgrounded_files_tab_does_not_claim_the_placement_pass` |
| TP-FMR-PLUGIN-HL-01 | The headless scheduled loop consumes a revalidated file-manager plugin intent and runs it through the existing command runtime exactly once. | The file manager's context menu is inert over a socket: the menu opens, the choice prepares an intent, and nothing ever consumes it — so the action silently does nothing. | `headless_scheduler_runs_file_manager_plugin_intent_once` |
| TP-FMR-PLUGIN-HL-02 | An intent whose Files surface has closed is consumed without executing, so it cannot fire later against a reopened directory. | A menu choice prepared before close executes after a same-directory reopen, against paths that only look the same. | `headless_scheduler_drops_plugin_intent_whose_files_surface_closed` |

## Pane bottom-border actions and the attachment picker

Fork affordances drawn into upstream's pane chrome. Geometry that upstream reflows is the risk; every row insists the action stays bounded, exact and no-color safe.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-M1.1-GEOMETRY | Only the exact focused agent may own one complete bottom-border action, and capability failures must not leave stale geometry behind. | An unfocused pane offers an action, or a dead affordance stays clickable after the capability went away. | `focused_agent_attachment_action_is_exact_agent_only_and_responsive` |
| TP-M1.1-RENDER | Render consumes computed geometry only; the ASCII action preserves pane corners and terminal inner cells even with no-color tokens. | The pane border breaks, or the action is invisible on a no-color terminal. | `agent_attachment_action_render_is_bounded_ascii_and_no_color_safe` |
| TP-M2.1-GEOMETRY | One eligible focused agent gets a complete worktree launcher beside — never overlapping — the existing attachment action. | The two actions overwrite each other and one becomes unclickable. | `focused_agent_worktree_action_is_capability_gated_and_disjoint` |
| TP-M2.1-RENDER | The worktree launcher is a bounded ASCII token that stays distinct from `[+]` and preserves pane corners in no-color mode. | Two different actions look identical, so the user cannot tell which one they are about to trigger. | `focused_agent_worktree_action_render_is_ascii_and_no_color_safe` |
| TP-M2.1-ROUTE | The frame action emits only the existing open intent and enters the established searchable dialog without creating new authority. | A second, divergent worktree dialog path appears and drifts from the canonical one. | `focused_agent_worktree_action_routes_to_existing_open_dialog_without_new_authority` |
| TP-M2.1-FAILURE | A source that disappears after frame computation fails closed in the existing dialog owner and preserves every agent resource. | A vanished worktree source tears down agent resources that had nothing to do with it. | `worktree_action_list_error_preserves_agent_resources_and_clears_only_request` |
| TP-M1.2-OPEN | Picker state binds the exact stable identities and starts from the same workspace cwd authority as the file manager. | The picker browses a different directory than the file manager, against a target it identified loosely. | `opening_attachment_picker_binds_exact_target_and_workspace_cwd` |
| TP-M1.2-TINY | Incomplete modal geometry declines before allocating picker or file-manager state and returns one stable visible-reason classification. | A tiny terminal allocates state for a modal it cannot draw, then fails somewhere less recoverable. | `attachment_picker_tiny_area_declines_with_visible_reason` |
| TP-M1.2-UNAVAILABLE | Capability loss fails closed with a stable visible reason instead of silently consuming the configured action. | The keybinding appears dead with no explanation. | `attachment_picker_unavailable_target_is_visible_and_non_mutating` |
| TP-M1.2-CANCEL | Cancelling the overlay owns no runtime resource and restores terminal mode without preparing a delivery request. | Escape leaks a resource, leaves the terminal in raw mode, or still sends the attachment. | `attachment_picker_escape_restores_valid_focus_without_delivery` |
| TP-M1.2-AUTHORITY | Exactly one current regular UTF-8 file is exposed as attachment authority; directories are navigation targets, never attachment authority. | A directory is handed to an agent as if it were a file. | `attachment_picker_accepts_one_regular_file_and_disables_other_targets` |

## Client lane scheduling

The client's event lanes. Upstream owns the loop; the fork's rule is that a burst of presentation frames must never delay the user's own input.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-FMP-CLIENT-01 | An exact input event cannot sit behind a stale burst of complete semantic presentation frames. | Typing lags behind a redraw storm, so keystrokes land seconds after they were pressed. | `fmp_client_input_precedes_semantic_frame_backlog` |
| TP-FMP-CLIENT-02 | Complete semantic frames are replaceable snapshots: the client retains the newest one instead of replaying every stale intermediate frame. | The UI animates through a backlog of obsolete states before showing the current one. | `fmp_client_semantic_frame_burst_keeps_only_newest_snapshot` |
| TP-FMP-CLIENT-03 | Incremental terminal frames are not complete snapshots, so they remain lossless and ordered on the control lane. | Terminal output is coalesced like a snapshot and characters are lost. | `fmp_client_terminal_frames_remain_lossless_and_ordered` |
| TP-FMP-CLIENT-04 | Input-first does not mean control starvation: after one bounded input quantum, one ready ordered event must make progress. | Sustained input starves the control lane and the session stops responding to anything else. | `fmp_client_input_quantum_yields_to_ordered_control` |

## Server writer lanes

The server's own side of the same rule, in `src/server/client_transport.rs`. The
queue was already correct when this row was written; what it lacked was a name,
which is the only thing that stops a merge from taking upstream's version of the
module and leaving a plain FIFO behind. A plain FIFO compiles, passes everything
else, and turns a burst of frames into input lag.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-CLIENT-WRITE-PRIO-01 | The client writer queue is strictly prioritised: a queued control frame is always drained before any render, and a render that was dropped for a newer one is reported rather than silently lost. | Control traffic queues behind presentation frames, so resize, shutdown and notification messages arrive late or out of order behind a redraw burst — and the drop of a stale render becomes invisible. | `client_writer_prioritizes_control_and_reports_render_drain` |

## Handshake capability negotiation

`Hello.capabilities` / `Welcome.accepted` in `src/protocol/wire.rs`, negotiated in
`src/server/client_transport.rs` and kept by `src/client/mod.rs`. This is the
fork's mechanism for adding media features without a protocol bump per feature;
the 20 → 21 bump, which cost the whole client fleet for one boolean, is what it
replaces.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-MEDIA-CAP-01 | The server accepts only the intersection of what the client announced and what it can itself serve, and the session carries exactly that set. | The server reflects the client's announcement back and promises capabilities it has no code for; or it accepts a capability and forgets it, so the client believes a feature was granted while the session has no record. Neither fails at handshake time — both fail later, inside whichever feature first relies on it. | `the_handshake_accepts_only_capabilities_both_sides_support` · `accepted_capabilities_travel_with_the_connected_event` · `capability_intersection_drops_what_only_one_side_offers` |
| TP-MEDIA-CAP-02 | An unknown capability name costs nothing: it is ignored, the handshake still succeeds, and adding a name only lengthens the frame instead of moving the fields around it. | The negotiation stops being additive. Every new media capability then needs a wire change and another `PROTOCOL_VERSION` bump, which breaks every released client — exactly the loop this mechanism was built to end. | `an_unknown_capability_name_is_ignored_rather_than_fatal` · `a_client_announcing_only_unknown_capabilities_still_connects` · `adding_a_capability_does_not_shift_the_wire_shape` |
| TP-MEDIA-CAP-03 | A client that announces nothing gets the pre-negotiation behaviour end to end: empty announcement, empty accepted set, empty session record. | The default path silently changes for every existing client, which is the one outcome a compatibility mechanism may never produce. | `an_empty_announcement_is_accepted_as_the_pre_negotiation_case` · `an_empty_capability_announcement_survives_a_roundtrip` |
| TP-MEDIA-WIRE-01 | Media messages are appended after every existing variant of `ServerMessage` and `ClientMessage`, and the wire tags of the variants before them are pinned by test. The codec travels as a name from the shared `codec` module rather than as an enum. | bincode is positional: a variant inserted anywhere but the end re-labels every message after it, and the failure compiles and ships — a released client decodes one message as its neighbour (this fork already paid for it once with a 9.4 second timeout nobody could explain). If the codec half is lost, a codec discriminant an older client has never seen kills the whole message rather than one refusable stream, turning a graceful decline into a dead connection. | `media_variants_are_appended_after_every_existing_one` · `media_messages_survive_the_wire_field_for_field` · `an_unknown_codec_name_decodes_instead_of_killing_the_message` · `an_audio_chunk_larger_than_a_control_frame_is_refused_not_decoded` |
| TP-MEDIA-PRIO-01 | The client writer queue drains control, then ordered renders, then the newest render, then media — media strictly last — and the media lane is bounded, refusing rather than growing when full. Close clears it with the others. One `try_pop` writes that order; `recv` blocks on top of it and the tests read it directly. | Media ahead of a render makes the interface slower the moment a stream opens, which is the one outcome this phase may not produce; and an unbounded lane turns a slow link into a delay that grows without ever recovering, so the client's playout never catches up. If `try_pop` is duplicated instead of shared, a test can agree with an order the writer thread does not use — green, and wrong. A media lane that survives close holds bytes for a client that is gone. | `the_media_lane_drains_after_every_other_lane` · `a_full_media_lane_still_lets_control_through_first` · `closing_the_writer_clears_the_media_lane_too` · `client_writer_prioritizes_control_and_reports_render_drain` |
| TP-MEDIA-CAP-06 | A shared capability *name* is not a shared *value*. `intersect` keeps a name whose values do not overlap — it must, because a flag capability has no values and an empty list is therefore not evidence of disagreement — so valued capabilities are read through `negotiated_value`, which answers `None` when there is no usable agreement, while flags keep being read with `has`. | "audio sink: opus" negotiated against "audio sink: pcm" leaves `has` answering true with no codec in common, and a caller that trusts it opens a stream the far side cannot decode a byte of. Nothing about that failure is loud: the handshake succeeds, the stream opens, chunks flow, and the only symptom is silence. If the flag half is lost instead, `None` gets read as "not supported" and every valueless capability quietly stops working. | `a_shared_name_with_no_shared_value_is_not_a_usable_capability` · `a_negotiated_value_is_the_first_the_client_named` · `a_flag_capability_has_no_negotiated_value_and_that_is_not_a_failure` |
| TP-MEDIA-CODEC-01 | Audio is Opus at 48 kHz stereo in 20 ms frames, constant bitrate, behind a seam that owns the codec choice. A frame in is a frame out; a wrong-sized frame, an out-of-range bitrate and a too-small output buffer are each refused rather than padded, clamped or partially written. The codec is unconditional — the `media-sink` flag covers the audio *device*, not encoding. | A decoder returning a different sample count, a padded short frame or a truncated long one each shift the stream by a fixed amount per frame: drift with no source to trace, inaudible once and obvious after a minute. A clamped bitrate makes every bandwidth measurement wrong while the audio still decodes perfectly — including the measurements this phase's acceptance rests on. If the flag covered the codec instead, a server built without it could not serve audio at all and would answer the capability handshake differently for a reason no client can see. | `a_frame_survives_encoding_with_its_sample_count_intact` · `a_wrong_sized_frame_is_refused_rather_than_padded` · `a_bitrate_outside_the_supported_range_is_refused_at_construction` · `a_too_small_output_buffer_is_refused_not_silently_truncated` · `a_second_of_audio_at_the_default_bitrate_costs_what_the_design_measured` |
| TP-MEDIA-CLOCK-01 | The client learns the server's clock from NTP's four timestamps, keeps the median of the last nine readings, and throws the whole history away after a minute without a probe. An unsynced clock reports that it is unsynced rather than answering zero. | A sign error in the one line of arithmetic is invisible: the stream plays, permanently early or late by twice the network delay, and looks like a slow link. An average instead of a median carries a single delayed reply's error forever, spread thin. Without the staleness reset a suspended laptop wakes with a jumped clock and a median built from before the jump outlives it by nine probes — playback wrong by the size of the jump, with nothing reporting it; this fork's dormancy feature makes long gaps ordinary. Offset zero is a legitimate answer, so it cannot also mean "no measurement yet". | `a_symmetric_probe_recovers_the_offset_exactly` · `an_unsynced_clock_says_so_rather_than_guessing_zero` · `one_delayed_probe_does_not_move_the_offset` · `a_gap_longer_than_a_minute_throws_the_old_samples_away` |
| TP-MEDIA-JITTER-01 | Interarrival jitter follows RFC 3550, updated only on in-order chunks and measured against each chunk's real `pts_us` rather than an assumed frame rate. The playout target is three times the estimate, clamped to 10..500 ms, and moves by a smoothed fraction per update. | Counting a reordered chunk inflates the delay in response to something the buffer already absorbs. Assuming a frame rate makes the estimate wrong exactly when the source's rate is not the assumed one. Without the clamp the phase's 800 ms end-to-end target is unreachable, since the buffer is its largest term; and without the smoothing the delay jumps, which is a playback *rate* change — audible where a delay is not. An estimate that rises and never falls raises the delay for the rest of the session after one bad moment. | `perfectly_regular_arrivals_produce_no_jitter` · `a_late_arrival_raises_the_estimate_and_a_calm_link_lowers_it_again` · `an_out_of_order_chunk_is_not_treated_as_jitter` · `the_playout_target_stays_inside_its_bounds` · `the_playout_target_moves_slowly_rather_than_jumping` · `an_early_chunk_waits_and_a_due_one_does_not` · `a_chunk_past_its_frame_is_expired_and_due_is_not_the_same_question` |
| TP-MEDIA-PLAYOUT-01 | A chunk is played at `pts_us + target_delay`, never on arrival. Chunks are held keyed by presentation time so they play in order whatever order they arrive in; one past its moment is refused at the door and one that expires while waiting is discarded rather than blocking what is behind it; a sequence gap is reported before the chunk after it is handed over; a duplicate is not played twice. | Playing on arrival hands every network wobble straight to the speaker, which is the failure the whole layer exists to prevent. Keying on arrival order defeats it a second way. An expired chunk that is played moves the stream back one frame and keeps it there; one that is held turns a brief stall into permanent silence. Loss the caller cannot see is loss it cannot conceal, and an unconcealed gap is a click rather than a quiet moment. | `an_early_chunk_is_held_until_its_moment` · `a_chunk_that_arrives_after_its_moment_is_refused_at_the_door` · `chunks_that_arrive_out_of_order_play_in_order` · `a_missing_chunk_is_reported_before_the_one_after_it_plays` · `a_duplicate_chunk_is_not_played_twice` · `a_chunk_that_expires_while_waiting_is_discarded_rather_than_blocking` · `a_steady_stream_keeps_the_target_delay_from_growing` |
| TP-MEDIA-SOURCE-01 | The server drops a late chunk at the source, before encoding, and sends nothing to a client with no credit. Time and sequence advance through a drop; a rejected malformed frame consumes neither. Credit is a level the client reports, not a total the server accumulates, and a dropped chunk does not spend it. | A queue that holds late chunks delivers every one eventually, each later than the last, and the delay never comes back down after the link does — bufferbloat, and the source is the only place the deadline is still known. If time paused during a drop, everything after an outage would present late by the length of the outage rather than the stream being briefly thin; if the sequence closed up, the client would see an unbroken stream and never learn anything was missing. Without credit the server's memory grows instead of the client's queue, so the first symptom of a slow client is the server. An accumulating counter would let one lost or duplicated credit message shift the server's idea of the far end permanently. | `a_chunk_past_its_moment_is_dropped_before_it_is_encoded` · `a_dropped_chunk_still_consumes_its_slot_in_time_and_sequence` · `a_client_with_no_room_is_not_sent_to` · `credit_is_spent_one_chunk_at_a_time_and_replaced_not_accumulated` · `one_frame_of_lateness_is_still_worth_sending` · `a_wrong_sized_frame_is_refused_without_consuming_a_slot` |
| TP-MEDIA-CAP-04 | The server announces `media.streams` and `media.audio.sink` with the codec it produces; the client announces `media.streams` but not a sink it does not have. The intersection therefore grants streams and withholds audio until a client can actually play it. | Announcing a name with no code behind it is the failure the intersection prevents one level up, in the other direction: a server told there is a sink encodes and sends chunks to a listener who will never hear them, and both ends look healthy while the middle is silent. Announcing only that a sink exists, without naming the codec, leaves the two sides to agree on the name and disagree on the content. | `the_server_announces_the_media_vocabulary_it_can_serve` · `a_client_with_no_sink_is_not_granted_one` · `the_client_announces_the_capabilities_it_can_actually_serve` |
| TP-MEDIA-CLOCK-02 | A `TimeSync` probe is answered on the reading thread, from the control lane, without passing through the event loop; the server takes its receive and send stamps separately. | A probe that queued behind application work would measure the event loop's backlog and the client would read it as network delay, then size its jitter buffer for a number that has nothing to do with the link. Nothing about that is visible — the reply arrives, the arithmetic works, the answer is wrong. Reusing one stamp for both fields reports the server's own processing time as zero and folds it into the same estimate. | `a_clock_probe_is_answered_on_the_reading_thread` |
| TP-MEDIA-SINK-01 | Every audio sink refuses a wrong-sized frame and reports itself closed after `close`; the silent sink counts what it accepted so a pipeline with no output device stays measurable. | A sink that pads or truncates shifts playback by a fixed amount per frame — the encoder's drift, at the other end of the pipe. A sink that accepts after close is the failure that looks like success: counters move, the pipeline runs, nothing plays. | `every_sink_refuses_a_wrong_sized_frame` · `a_closed_sink_says_so_rather_than_accepting_silently` |
| TP-MEDIA-SINK-02 | The external-process sink writes interleaved little-endian f32 in frame order to the player's stdin, closes by dropping stdin rather than killing, and turns a player that exited into `Closed` instead of a panic. | The wire to a player is a headerless byte stream both sides must agree on without being able to check; what we send is the only half this repository owns. Killing the player cuts what it already buffered, audibly. A dead player taking the client down turns a user quitting `ffplay` into a lost session. | `an_external_sink_writes_little_endian_f32_in_frame_order` · `a_player_that_exits_turns_the_sink_closed_instead_of_panicking` |
| TP-MEDIA-SINK-03 | Player selection takes the first program that starts, in a shipped order that names the sample rate and channel count on every command line, and a total failure lists every program it tried. | Selection is tested with players the test owns, because the shipped names are present or absent depending on the machine and `mpv` — the one the design named — is not installed here. Rate and channels appear twice, once as codec constants and once as command-line text; a disagreement is inaudible in the usual way, the player resamples and the audio plays at the wrong speed. "No audio output" without the list is a dead end for whoever reads the log. | `selection_takes_the_first_player_that_starts_and_reports_the_rest` · `no_usable_player_names_everything_it_tried` · `the_shipped_player_list_agrees_with_the_stream_shape` |
| TP-MEDIA-PLAYBACK-01 | A received chunk reaches the sink at `pts + target_delay`, never on arrival; the playout core is told the time rather than reading it. | Writing on arrival hands every network wobble to the speaker; a core that read the clock itself could not be tested against one, and the rule would be trusted instead of pinned. | `a_chunk_reaches_the_sink_at_its_moment_and_not_before` |
| TP-MEDIA-PLAYBACK-02 | A missing chunk is replaced with a frame of silence rather than skipped, at most five in a row; past that the stream resumes from what arrived. | Skipping pulls every later frame early — the stream drifts ahead of the picture by one frame per loss. Unbounded concealment plays a second of nothing while newer audio already waits in the buffer. | `a_missing_chunk_is_concealed_with_silence_rather_than_skipped` · `concealment_is_bounded_so_a_long_gap_resumes_instead_of_playing_silence` |
| TP-MEDIA-PLAYBACK-03 | Credit is the room left in the playout buffer below `CREDIT_ROOM`: it falls as chunks are held and rises as they play. | Credit is a level the server trusts. A level that did not fall would flood a stalled client; one that did not rise would starve a healthy one after its first burst. | `credit_is_the_room_left_in_the_buffer` |
| TP-MEDIA-PLAYBACK-04 | An underrun is counted when a frame has played, the buffer is empty and the successor's moment has passed by more than two frames — once per gap, never before the first frame. | The phase's first acceptance criterion is "zero underruns in an hour"; a counter that fired before the stream started, or on every tick of one silence, would make that number meaningless in both directions. | `running_dry_mid_stream_counts_one_underrun_per_gap` |
| TP-MEDIA-PLAYBACK-05 | A packet that fails to decode is concealed like a lost one and the stream continues. | Stopping lets one corrupted byte end the stream; skipping without silence pulls every later frame early. | `a_corrupt_packet_is_concealed_and_the_stream_continues` |
| TP-MEDIA-PLAYBACK-06 | A sink that fails stops playback, refuses further chunks and reports zero credit. | Credit zero is the only "stop" this protocol has. A dead sink still advertising room has the server encode and send audio into nothing, healthy at both ends. | `a_sink_that_dies_stops_playback_and_zeroes_the_credit` |
| TP-MEDIA-PLAYBACK-07 | The playout thread builds the sink on its own thread at the first open, blocks outright while no stream is open, plays due chunks on a 5 ms tick and publishes credit, counters and the sink's name for the main loop; a sink that cannot open declines the stream with zero credit. | A native audio stream is not always `Send`, and building it where it is fed means it never has to be. A thread that polled with nothing open would cost an idle client wakeups it does nothing with — the resource rule this fork holds every component to. A failed sink that stayed silent would leave the server sending forever. | `the_playout_thread_plays_a_due_chunk_and_publishes_what_it_did` · `a_sink_that_cannot_open_declines_the_stream_with_zero_credit` |
| TP-MEDIA-RING-01 | The sample ring between the playout thread and a native audio callback is lock-free single-producer/single-consumer: it refuses when full rather than overwriting, zero-fills what the callback cannot get and reports how much was real, and keeps order across the wrap and across threads. | The callback runs on a real-time thread that must never wait; a mutex that is "almost always" free is a click at the worst moment. Overwriting drops the oldest audio, which is the audio about to play. Wrap-around errors are silent — samples out of order sound like a click, not a panic. | `samples_come_out_in_the_order_they_went_in` · `a_full_ring_refuses_rather_than_overwriting` · `an_empty_ring_fills_the_callback_with_silence_and_says_how_much_was_real` · `the_ring_wraps_without_losing_a_sample` · `producer_and_consumer_agree_across_threads` |

## Notes for the next sync

- `TP-REPAINT-2B` is the only row here that asserts something *does not* happen. A
  resolution that makes every mouse move repaint will pass every other test in the
  suite.
- `TP-FMR-SIDEBAR-HL-01..03` are pinned by a single live headless test on purpose:
  the App-level equivalent cannot reach the real remote-client boundary.
- The `TP-ACT-*` and `TP-REPAINT-*` families were invisible to the registry until
  2026-07-25 — their markers did not follow `TP-<FAMILY>-<NN>` and the checker
  truncated them into `TP-A` and dropped `TP-2B..2F` entirely. Both families sit in
  upstream-owned files, which is exactly the bucket where invisibility costs most.

## Per-client focus

One session, several displays. Upstream keeps a single active tab per workspace, so
every one of these rows sits in a region a sync can revert to "one tab for everyone".
That revert compiles, passes upstream's tests, and destroys the reason this fork's
users keep several terminals open at once — the failure is invisible until two
displays are attached at the same time.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-MCF-CTX-01 | Entering a viewer returns the previous one, and restoring it puts that one back, so windows nest. | A render pass that wraps an input routing call loses its own view, and draws the wrong client's tab. | `entering_a_viewer_returns_the_previous_one_for_restoration` |
| TP-MCF-CTX-02 | The viewer installed on `AppState` reaches every workspace. | Workspace accessors resolve for nobody, so every display silently falls back to the shared default. | `the_viewer_context_reaches_every_workspace` |
| TP-MCF-CTX-03 | Routing one client's input opens that client's view and puts the previous one back on the single exit path. | A tab switch is recorded against the wrong display, or a view leaks past the event that installed it. | `routing_a_client_scopes_the_viewer_and_puts_the_previous_one_back` |
| TP-MCF-CTX-06 | A frame's graphics are encoded inside the same viewer window the frame was rendered in. | The encode reads whichever view the restore installed — the session default, whose owned surfaces hold no file browser — so the moment a second display attaches, every file-manager preview silently stops arriving. Found live. | `fm_image_graphics_reach_the_display_showing_files_with_another_display_present` |
| TP-MCF-CTX-07 | The deferred-request pass runs inside the foreground display's viewer window: a queued request is that display's own click, consumed later. | A + press queues a new tab under one display's window and the pass consumes it under none — the focus move lands in the session's default registers, the display that asked watches nothing happen, and displays that never asked follow the default. The user's live report: a new tab opened, the clicking client's focus stayed put. Same resolution TP-SUR-BROADCAST-05 pinned for plugin-raised popups. | `a_deferred_new_tab_lands_in_the_display_that_asked_for_it` |
| TP-MCF-CTX-04 | A workspace created inside an open viewer window carries no viewer until the next one, and resolves the default meanwhile. | A workspace created mid-event either panics on an absent client or adopts a tab index that does not exist yet. | `a_workspace_created_inside_a_viewer_window_falls_back_to_the_default` |
| TP-MCF-TAB-01 | Two clients hold different active tabs in one workspace at the same time. | The session collapses back to one focused tab for every display: the last click drags every other terminal onto its tab. | `two_clients_hold_different_active_tabs_at_the_same_time` |
| TP-MCF-TAB-02 | A departed client's tab is dropped, and the clients that stay do not move. | Closing one terminal jumps the remaining terminals to a different tab, and the departed client keeps a slot that later tab removals must maintain. | `losing_one_client_leaves_the_others_where_they_were` · `a_display_that_detaches_does_not_move_the_one_that_stays` |
| TP-MCF-TAB-03 | Closing a tab moves only the display that closed it; the others keep looking at the same tab, not the same index. | Every other display silently shifts to a neighbouring tab whenever anyone closes one. | `closing_a_tab_moves_only_its_own_viewer_and_keeps_the_others_on_the_same_tab` |
| TP-MCF-TAB-04 | A client with no tab of its own adopts the workspace default, which tracks the most recent explicit switch. | A newly attached display opens on tab one instead of where the session is actually being worked, and a restored session opens on the wrong tab. | `a_client_without_a_slot_adopts_the_workspace_default` |
| TP-MCF-TAB-05 | Switching tabs on one display through the real input path leaves an untouched display where it was. | The end-to-end path regresses even while the storage stays per-client, because the switch was recorded outside any viewer window. | `one_display_switching_tabs_leaves_the_other_display_where_it_was` |
| TP-MCF-TAB-06 | One client holds a separate tab in each workspace. | Switching workspace and coming back lands on the wrong tab, so a display loses its place whenever the user looks elsewhere. | `one_client_holds_a_separate_tab_in_each_workspace` |
| TP-MCF-TAB-07 | A display that detaches does not move the display that stays. | Closing a second terminal yanks the first one onto another tab. | `a_display_that_detaches_does_not_move_the_one_that_stays` |
| TP-MCF-SIZE-01 | A tab is sized to the smallest display watching it, so a tab watched by one display keeps that display's full size, and another display's background sweep leaves a watched tab alone. | Pane size follows whichever display was touched last, so the tab a user is working in is resized by a display they are not even looking at. | `each_display_sizes_the_tab_it_alone_is_watching` |
| TP-MCF-SIZE-02 | A tab watched by two displays fits the smaller of them. | The shared tab is sized for the larger display and the smaller one clips its content. | `render_and_stream_uses_each_client_terminal_size` |
| TP-MCF-SIZE-03 | Background tabs are reconciled by size-change events, never by client render passes, so a steady frame applies zero background resizes. | Every render pass rewrites every unwatched tab to whichever display happens to be drawing; two displays disagree every frame, and each rewrite reflows that pane's whole scrollback through libghostty-vt — measured live at 75% of the server main thread's stack samples and ~16% of a core per extra display. | `a_steady_frame_costs_no_background_resize` |
| TP-MCF-SIZE-04 | Moving input focus between displays reconciles the newly foreground display's own tab and nothing else, so alternating between two windows applies zero background resizes. | A change of focus is treated as a change of session size: every unwatched tab is rewritten to whichever window was last typed in, so working across two windows reflows the whole session's scrollback once per alternation. | `moving_input_between_displays_costs_no_background_resize` |
| TP-PANE-RETIRED-01 | The background sweep leaves a pane whose child has been reaped alone; it is sized again when it becomes the tab on screen. | A terminal with no process left is reflowed on every session size change — the most expensive operation in the pane, spent rewrapping scrollback for a child that cannot receive the new size and a viewer who is not there. | `a_background_pane_whose_child_exited_is_not_resized` |
| TP-PANE-RETIRED-02 | A pane's liveness is answered by its reaping record *and* by the OS, so a pane imported through a live handoff is still recognised as retired once its process is gone. | `from_handoff_fd` keeps the child's pid but not the reaping record — the task that would set it died with the previous server — so after any in-place update every pane reports as running forever, and no cost decision that rests on liveness can ever fire. | `a_handoff_pane_whose_process_is_gone_counts_as_retired` |
| TP-MCF-SIZE-05 | Size negotiation identifies a tab by its workspace and its index together, so displays in different workspaces never negotiate against each other. | Every workspace's tab 0 is one tab as far as negotiation is concerned: a display in another workspace drags an untouched display's panes down to its own width — measured at 59 columns inside a 200-column window — and the display that lost the ownership tie stops being sized at all. | `same_tab_index_in_different_workspaces_sizes_independently` · `tabs_in_different_workspaces_do_not_share_a_size_negotiation` |
| TP-MCF-SIZE-06 | A tick with no display attached computes pane geometry once — so the API and the first attach see real sizes — and never draws: PTY output alone does not buy a frame nobody sees. | Detached servers render and discard a full virtual frame for every burst of PTY output, up to 62 a second; 45 stray servers measured 30.6 hours of CPU drawing frames with no viewer. | `frames_with_no_attached_client_are_computed_once_not_per_tick` |
| TP-HANDOFF-FD-01 | The live-handoff fd budget clears sessions that actually exist: 65 pane fds cross one handoff (the cap itself is `MAX_FDS_PER_HANDOFF` = 128; 65 is the test's probe, a floor — read the ceiling from the constant, not from this row). | A measured 59-pane session sat six panes from the old 64-fd refusal; the next `herdr update --handoff` past that line rejects the whole session, and the kernel's own SCM_RIGHTS ceiling of 253 shows the refusal was policy, not physics. | `a_sixty_five_pane_session_fits_through_one_handoff` |
| TP-HANDOFF-HIST-01 | An agent pane's scrollback survives a live handoff through the freight file. | The export suppressed agent-pane history entirely — a rule copied from cold restore, where the relaunched agent repaints its own transcript. A handed-off agent is the same process on the same PTY and repaints nothing: two live agent panes were measured reduced to one line of scrollback by a morning update. | `live_handoff_preserves_agent_pane_scrollback` |
| TP-HANDOFF-HIST-02 | A pane caught on the alternate screen keeps its primary scrollback across a live handoff: the capture reads the primary screen directly (vendor patch 0002), never leaks the alternate frame into history, and the import replays history before it seeds the input state so the replay lands on the primary screen the fresh core starts on. | This was the last handoff data-loss hole: the capture could only read the active screen, so a pane sitting inside a fullscreen app lost 100% of its primary scrollback on every `herdr update --handoff` — and the import's seed-then-replay order would have flushed even a carried history into the alternate screen. | `live_handoff_preserves_primary_history_of_an_alt_screen_pane` · `handoff_history_ansi_full_reads_the_primary_screen_from_the_alternate` · `reading_the_primary_screen_while_the_alternate_is_active` |
| TP-HANDOFF-HIST-03 | History longer than the 8 KiB inline manifest replay survives a live handoff; the freight file is the carrier, the inline replay only the fallback for importers that predate it. | The manifest line is read one byte per syscall (0.77 MiB/s measured), so its replay budget cannot grow — every pane quiet since the last update keeps only its final ~40 rendered rows, and the session's history erodes on every `herdr update --handoff`. | `live_handoff_preserves_scrollback_beyond_the_inline_replay_budget` · `handoff_history_freight_roundtrips_full_pane_history` · `handoff_history_ansi_full_keeps_what_the_inline_budget_drops` |
| TP-DORMANT-01 | A pane whose child process is still running is never put to sleep — not by the API, not by quiet time, not by memory pressure. | Herdr's reason to exist is agents running for days unattended; a dormancy that can reach a live child is the product's core promise breaking, dressed as a resource optimisation. | `a_pane_with_a_live_child_is_refused_dormancy` · `the_sweep_never_reaches_a_live_pane_even_under_pressure` |
| TP-DORMANT-02 | A retired, unwatched pane's runtime is released: scrollback to disk first, then the PTY actor, reader, and terminal core go, while the pane and its terminal identity stay. | A session accumulates retired panes by design, and each one holds a PTY thread and a full scrollback in memory forever — 47 of 59 panes in a measured live session were exactly this. | `a_retired_unwatched_pane_goes_dormant_and_releases_its_runtime` |
| TP-DORMANT-03 | Waking a dormant pane replays the scrollback it went to sleep with — on the shell path; an agent pane's wake hands the launch to the resume machinery instead and drops the file unreplayed (TP-DORMANT-12). | Dormancy without faithful waking is deferred data loss: the pane comes back as an empty shell and the user's history is gone exactly when they came looking for it. The agent exception is the same faithfulness one layer up: the resumed agent repaints its own transcript, and a replay would double it — the rule cold restore already lives by. | `waking_a_dormant_pane_restores_its_history` |
| TP-DORMANT-04 | A tab a display starts watching wakes its dormant panes; API input to a dormant pane wakes it first. | Attach, tab switch, workspace switch, and focus all end at a watched tab — waking anywhere else leaves some path showing a permanently blank pane. | `a_watched_tab_wakes_its_dormant_panes` |
| TP-DORMANT-05 | Without memory pressure a retired pane must be a day quiet before it sleeps; pressure shortens the wait but never bends the live-child rule. | A pane that exited a minute ago may be mid-workflow — a respawn, a user about to click. Sleeping it early turns every shell exit into a surprise blank pane. | `the_sweep_waits_out_the_quiet_threshold_without_memory_pressure` · `memory_pressure_sleeps_a_retired_pane_without_waiting` |
| TP-DORMANT-06 | A dormant pane shows one dim static line saying it sleeps; an awake pane never shows it. | A silently blank pane is indistinguishable from a rendering bug, and an animated indicator would spend the very render passes dormancy exists to save. | `a_dormant_pane_renders_its_sleeping_notice` |
| TP-DORMANT-07 | A restored session replays a dormant pane's saved scrollback into its fresh shell and consumes the file. | A server restart orphans every dormant history file: the pane comes back as an empty shell, and the disk still holds the history it will never show — dormancy's promise broken at exactly the boundary it exists for. | `restore_replays_a_dormant_panes_scrollback_and_consumes_the_file` |
| TP-DORMANT-08 | Closing a dormant pane deletes its history file; the close path queues the orphaned path and a scheduled-task drain removes it in both loops. | The wake and the restore both consume the file, but a closed pane can never wake — every close leaked the file forever, and the days-open laptop dormancy exists for is exactly the session that accumulates them. | `closing_a_dormant_pane_deletes_its_history_file` |
| TP-DORMANT-09 | The dormant scrollback write is atomic: tmp, fsync, rename — a failure leaves no partial file and the refusal keeps the runtime. | `fs::write` begins with truncate; a crash mid-write leaves a torn file the wake silently replays as garbage, breaking TP-DORMANT-03's promise at the exact moment it matters. | `a_failed_dormant_write_leaves_no_partial_file` |
| TP-DORMANT-10 | A retired pane sitting on the alternate screen sleeps carrying its primary-screen history (on unix; the non-unix capture still refuses with `HistoryUnavailable`), and an empty retired pane still sleeps without a file. | The refusal was the lesser loss while the capture could only read the active screen; vendor patch 0002's screen formatter reads the primary scrollback directly, so a pane caught mid-fullscreen-app now keeps exactly the history the user would scroll back to instead of being pinned awake forever. The empty pane has nothing to lose and keeps its fileless sleep. | `an_alt_screen_retired_pane_sleeps_with_its_primary_history` · `an_alt_screen_pane_with_an_empty_primary_sleeps_without_a_file` · `an_empty_retired_pane_still_sleeps_without_a_file` · `a_popup_pane_cannot_become_dormant` |
| TP-DORMANT-11 | The quiet-time dormancy threshold is thirty-two hours, pinned by value; the memory-pressure early path is untouched. | Asked for on 2026-08-20: a 24-hour day was short enough that agents parked overnight and picked up the next afternoon had already been put to sleep. The pressure bypass stays because the requester said so in the same breath — "OOM can send them there, that is fine; I mean the default." Pinning the number keeps a merge from quietly shortening the day back. | `the_quiet_threshold_is_thirty_two_hours` |
| TP-DORMANT-12 | Waking a dormant pane that carries a persisted agent session queues its resume plan for the cold-restore resume machinery — no bare shell is spawned by the wake, the saved scrollback file is deleted unreplayed, and an agent the resume table does not know falls back to the shell path with its history intact. Both wake funnels (pane touch and watched-tab sweep) take the same path, including a pane that slept on the alternate screen. (TP-DORMANT-11 is reserved by the idle-shell v2 design.) | The user's click protocol: a dormant agent pane should come back as the conversation, not as an empty shell the user must relaunch by hand. The machinery, the transcript-repaint rule, and the plan table all exist for cold restore — waking through them keeps one launch path instead of two that drift apart. | `waking_a_dormant_agent_pane_queues_its_resume_and_drops_the_replay` · `a_watched_tab_wake_uses_the_resume_path_for_an_agent_pane` · `an_alt_screen_agent_pane_sleeps_with_history_and_wakes_into_a_resume` · `an_agent_session_without_a_resume_plan_wakes_into_the_shell_path` |
| TP-DORMANT-13 | The `pane.sleep` API verb puts a retired, unwatched pane to sleep through the same policy gate as the sweep: every refusal binds the caller as an API error (`child_still_running`, `pane_watched`, `history_write_failed`, `pane_not_found`), success answers with the pane's fresh info carrying `dormant: true`, and a repeated sleep is an idempotent success that changes nothing and emits nothing. | An API surface that bypassed `make_pane_dormant` would be a second dormancy authority: the unbendable live-child and watched-pane rules would hold on one path and not the other, and the first caller to find the difference would blank a watched screen or reach a live agent. | `the_pane_sleep_verb_puts_a_retired_unwatched_pane_to_sleep` · `the_pane_sleep_verb_never_reaches_a_live_child` · `the_pane_sleep_verb_refuses_a_watched_pane` · `the_pane_sleep_verb_is_idempotent_on_a_dormant_pane` · `the_pane_sleep_verb_reports_an_unknown_pane` |
| TP-DORMANT-14 | A pane's API info carries `dormant: true` when it sleeps and `alternate_screen: true` when a fullscreen app owns its viewport; both fields are omitted when false, so an awake primary-screen pane's JSON is byte-identical to what it was before the fields existed. | Without them the lifecycle runs unobserved: an API watcher polling the snapshot cannot tell a sleeping pane from an awake one, and the scroll guard's intent ("no scrollback yet" vs "a fullscreen app owns the viewport") cannot be read without a screen capture. Every dormancy transition announces itself as a pane.updated event — the sleep verb, the sweep, and both wake directions — so a subscribed watcher hears the change instead of discovering it on its next poll; a no-op (idempotent) sleep emits nothing. | `a_dormant_panes_api_info_says_so_and_an_awake_panes_does_not` · `an_alt_screen_panes_api_info_says_so_and_a_primary_panes_does_not` · `the_pane_sleep_verb_announces_the_transition` · `the_dormancy_sweep_announces_each_pane_it_sleeps` · `waking_a_dormant_pane_announces_it` |
| TP-SRV-RETIRE-01 | A server with any running pane child never retires, no matter how long nothing is attached. | The refuted 2026-08-15 draft — "server lives iff a direct child exists" — would have killed a measured live session whose panes were reparented by handoff while preserving twenty garbage servers. The pane ledger, not the process tree, answers liveness. | `a_server_with_a_live_child_never_retires` |
| TP-SRV-RETIRE-02 | An attaching client resets the idle clock to zero. | A session someone touched half an hour ago is a session someone wants; retiring on stale idle time turns a coffee break into a lost session. | `an_attached_client_resets_the_retirement_clock` |
| TP-SRV-RETIRE-03 | A clientless server whose every child has exited retires after the grace period, writing retired scrollback to disk and saving the session first. | 45 orphan servers were measured burning 30.6 hours of CPU drawing frames nobody saw; a session disk can fully hold has no claim on a process. | `a_childless_clientless_server_retires_after_the_grace` |
| TP-SRV-HK-01 | The headless housekeeping pass — scheduled tasks plus deferred requests — runs at most once per 10ms, however often PTY output wakes the loop. | The pass swaps every display's surface registers; paid per wake it multiplies with PTY event rate, and a live 27-pane server was measured pinning one full core (98% main-thread) doing nothing a person could see. | `housekeeping_pass_is_rate_limited_within_the_min_interval` |
| TP-SRV-HK-02 | A skipped housekeeping pass is retried within the interval, and a pass that ran schedules no wake at all. | Without the retry, deferred click-raised actions can stall up to the 250ms accept poll; without the no-wake half, an idle server heartbeats every 10ms forever — the exact cost RD2 forbids. | `a_skipped_housekeeping_pass_schedules_a_wake_within_the_interval` |
| TP-SRV-PROF-01 | A config-dir `render-prof.on` marker arms the render profiler when the environment variable is absent, and the variable stays authoritative in both directions. | A live-handoff child inherits the old server's environment, so an env var alone can never arm profiling on a live server — the 2026-08-25 spin hunt needed exactly this and had no way in. | `render_profiling_marker_file_requests_profiling`, `render_profiling_env_overrides_the_marker_file` |
| TP-SRV-HK-03 | A past-due loop deadline is clamped to the housekeeping cadence before the loop parks on it. | Every select-chain deadline is consumed by the housekeeping pass; parking on one already in the past returns immediately, and a single unserviced deadline was measured turning the live loop 167,000 times a second at 95% of a core. | `a_past_due_loop_deadline_is_clamped_to_the_housekeeping_cadence` |
| TP-MCF-WS-01 | Two clients stay in different workspaces at the same time. | Tabs in different workspaces still drag each other, so per-tab focus is undone one level up. | `two_clients_stay_in_different_workspaces` |
| TP-MCF-WS-02 | A client that never moved adopts the workspace the session is being driven to, and a departed client leaves no slot. | A display attaching later opens on workspace zero instead of where work is happening, and departed clients accumulate. | `a_client_that_never_moved_adopts_the_workspace_the_session_is_driven_to` |
| TP-MCF-WS-03 | An empty per-client workspace slot is the absence of a choice, so a client that attached before any workspace existed follows the first one created. | That display resolves to no workspace forever; its renders then resize live panes to a fallback area, which shrinks a detached session's PTYs. | `a_client_that_attached_before_any_workspace_existed_follows_the_first_one` |
| TP-MCF-VIEW-01 | A pointer event is resolved against the geometry of the display that sent it, not against whatever the last render left in the shared view. | Clicks from one display land on another display's layout — a different tab at a different size — so the wrong pane, row or tab is hit and nothing looks obviously wrong. | `a_pointer_event_is_resolved_against_its_own_display` |
| TP-MCF-VIEW-02 | Correct hit geometry costs at most one layout pass per pointer burst, and none at all for the display the last frame already described. | A layout pass lands on the serial input loop for every pointer motion, so the pointer feels heavy — the exact cost the inert-motion render gate was added to remove. | `a_pointer_burst_recomputes_geometry_once_and_the_foreground_never_pays` |
| TP-MCF-PANE-01 | Two displays on the same tab focus different panes. | They steal each other's focused pane, so a keystroke typed on one display is delivered to the pane the other display selected. | `two_clients_on_one_tab_focus_different_panes` |
| TP-MCF-PANE-02 | A display whose focused pane was closed by another display falls back to a pane that exists. | It resolves to a pane no longer in the tree, so that display focuses nothing and its input goes nowhere. | `a_client_whose_focused_pane_was_closed_falls_back_instead_of_focusing_nothing` |
| TP-MCF-PANE-03 | A departed display leaves no focused pane behind in any tab. | Focus slots accumulate per tab, and a reconnecting client inherits a stale pane from the previous connection. | `a_departed_client_leaves_no_pane_focus_behind` |
| TP-MCF-MODE-01 | `session.per_display_focus = false` mirrors one view onto every display, reproducing the pre-feature behaviour exactly and recording no per-display view. | There is no way back for pairing or a demo, and no rollback if per-display focus turns out to be wrong for a user. | `mirror_mode_puts_every_display_back_on_one_view` |
| TP-MCF-MODE-02 | A session whose displays have all left resolves the shared default without panicking. | The server panics or resolves to nothing after the last terminal closes, taking the detached session with it. | `a_session_with_no_clients_resolves_the_default_without_panicking` |
| TP-MCF-UI-01 | Every display draws the tab it is on within one frame pass, so several monitors show different tabs of one session at the same time. | The feature is present in state but not on screen: displays resolve their own tab yet one of them draws another's, which is indistinguishable from the original bug. | `every_display_draws_its_own_tab_in_the_same_frame` |
| TP-SUR-DEFAULT-01 | Serving a display that did not move anything leaves the session default alone, so a pane in a background workspace keeps reading as background. | The render loop parks every display on every frame, so the default becomes "whatever was rendered last"; a background pane then reads as foreground and its agent finishes in silence. | `serving_a_display_that_did_not_move_leaves_the_session_default_alone` |
| TP-SUR-BROADCAST-01 | A surface the session changes with no display being served reaches every display. | Focusing a pane through the API puts the session in terminal mode while a display stays parked in navigate mode, so everything typed on it is swallowed. | `a_mode_the_session_sets_itself_reaches_every_display` |
| TP-SUR-BROADCAST-05 | An API request that can open a person-opened surface runs in the focused display's view. | A plugin opens its viewer by calling back into the API (`herdr plugin pane open --placement popup`), so the request carries no display identity. Handled with no viewer it lands in the session's registers, and the broadcast rule then copies that popup onto every attached display — one person's preview click covering every screen. Reported live, twice. Scoping the request to the focused display is what stops the broadcast from ever seeing it. | `a_plugin_popup_opened_through_the_api_belongs_to_the_focused_display`, `a_session_instruction_still_reaches_parked_displays` |
| TP-SUR-BROADCAST-02 | A workspace the session picks does not move the displays. | The counterpart to the broadcast rule collapses: an API workspace switch drags every display, undoing per-display focus. | `a_workspace_the_session_picks_does_not_move_the_displays` |
| TP-SUR-BROADCAST-06 | The rail's remaining broadcast members (sidebar tab, selected row, scroll offsets) are presentational: a session-level change to them reaches parked displays, while a person-opened surface in the same class is still not born on a display that never asked for it. | The class comment claimed this distinction but nothing stated it, so the next person to add a field here had only prose to go on — and getting it wrong is exactly how the popup reached every screen. | `the_rails_presentational_broadcast_members_reach_parked_displays` |
| TP-SUR-STAGE-01 | One display opening Files leaves the other in the terminal, and neither takes the other's surface when served. | Opening the file browser sends every display to it — the reported symptom, one level above the workspace, tab and pane that were already split. | `one_display_opening_files_leaves_the_other_in_the_terminal` |
| TP-SUR-STAGE-03 | A display browsing files keeps the file workers running even when the session's own view names the terminal. | The listing stops refreshing the moment the session's view names the terminal, while a display sits in Files watching a directory go stale. | `a_worker_outside_every_display_still_sees_files_open` |
| TP-SUR-STAGE-04 | A second display opening Files joins the instance already open instead of minting its own. | One set of contents carries two lifecycle identities, so every watcher event tagged with one is rejected as stale by the display holding the other. | `a_second_display_opening_files_joins_the_instance_already_open` |
| TP-SUR-STAGE-06 | Focusing a pane takes the acting display out of the file browser, and only that display. | Clicking an agent in the sidebar from Files moves the workspace, tab and pane focus correctly and appears to do nothing, because Files is still drawn on top. | `clicking_an_agent_from_the_file_browser_takes_that_display_to_its_pane` |
| TP-SUR-OVERLAY-01 | A menu opened on one display does not open on the others, and does not take their input. | Right-clicking on one monitor opens the menu on all of them — the complaint that started this phase. | `a_menu_opened_on_one_display_does_not_open_on_the_others` |
| TP-SUR-OVERLAY-02 | Each display types into its own naming prompt. | One display's keystrokes land in another display's dialog, so a rename typed on one screen renames what the other screen had selected. | `each_display_types_into_its_own_prompt` |
| TP-SUR-RAIL-01 | The left rail's tab, its selected row, its scrolls and its folded rows belong to each display. | Switching to Projects switches every display to Projects, and a scroll position — a statement about one viewport — is applied to viewports that never scrolled. | `the_left_rail_and_its_scroll_belong_to_each_display` |
| TP-SUR-GEOMETRY-01 | Sidebar width and collapse are measured against the display drawing them. | One width is imposed on two different-sized screens, and the collapse a narrow display needs is forced onto a wide one. | `sidebar_geometry_is_measured_against_the_display_it_is_drawn_on` |
| TP-SUR-GESTURE-01 | A display seen for the first time is not halfway through another display's drag. | A gesture anchored to a rectangle in one display's last frame is resolved against geometry another display never saw. | `a_display_seen_for_the_first_time_is_not_halfway_through_a_drag` |
| TP-SUR-FM-01 | Two displays browse two directories at once. | Both displays browse whichever directory the last one navigated to, so one person's file browser follows the other's. | `two_displays_browse_two_directories` |
| TP-SUR-FM-02 | Scheduled file work runs once per display, in that display's view, in a stable order. | The workers see only the session's registers, so they refresh a listing nobody is looking at while the ones on screen go stale — and which display wins a race changes between runs. | `scheduled_work_visits_each_display_in_its_own_view` |
| TP-SUR-FM-03 | Each display navigates its own browser, keeping its own directory and its own cursor row. | A cursor is a position on one screen; sharing it moves the other display's selection out from under whoever is reading it. | `each_display_navigates_its_own_browser_without_moving_the_other` |
| TP-SUR-FM-04 | A departed display leaves no browser behind and stops being served by the file workers. | A reconnecting client inherits a stale browser, and threads and channels accumulate for views nobody will ask about again. | `a_departed_display_leaves_no_browser_behind` |
| TP-SUR-ADOPT-01 | The first display served takes over what the session had open; a display attaching after that starts in the terminal. | Either the monolithic path loses a browser opened before any display existed, or plugging in a second monitor hands it the first one's open browser — the shared-focus complaint arriving on attach. | `a_display_attaching_later_does_not_inherit_an_open_browser` |
| TP-SUR-ADOPT-02 | A display attaching next to already-seen displays is born without person-opened overlays: no popup, no preview viewer, no context menu, no half-typed prompt, and never in an overlay mode whose overlay was stripped. The first display served is exempt — it IS the session and takes over what the session had open, monolithic behaviour unchanged. | After a restart every display re-attaches as "seen for the first time", so one screen's popup is adopted by all of them — the popup-on-every-display report. An overlay mode without its overlay eats the first keystrokes. | `a_display_seen_for_the_first_time_is_born_without_person_opened_overlays` |
| TP-SUR-STAGE-05 | Closing the browser on one display leaves the others browsing. | Closing one display's file browser takes away one another display is still reading, or leaves it showing a surface with nothing behind it. | `closing_the_browser_on_one_display_leaves_the_others_browsing` |
| TP-SUR-FM-05 | A navigation the rail asked for belongs to the display that asked, and is still waiting when that display is served. | Scheduled work serves displays lowest-id first, so a shared request is consumed by the wrong display: it navigates that one's browser and the display actually clicked sits inert. | `a_request_one_display_made_is_not_consumed_by_another` |
| TP-SUR-FM-06 | Every browser request — rename, bulk rename, delete, context action — belongs to the display that made it and is taken up in that display's view. | Each resolves against the asking display's rail focus, directory and selection; taken outside that view they match nothing and the rename or delete is silently dropped on every display but the first. | `every_browser_request_belongs_to_the_display_that_made_it` |
| TP-SUR-FM-07 | A context action is visible only in the view that raised it, and all three of its consumers — send-to-agent, plugin dispatch, ordinary actions — run in that view, in that precedence. | Consumers split across the loop boundary read nobody's browser once a second display attaches: the request is taken, nothing matches, and the whole branch goes quiet on every display. | `a_context_action_is_visible_only_where_it_was_raised` |

## Mobile and responsive layout

A phone reaching Herdr over SSH is a viewport, not a platform. The fork treats the
viewport's size as a first-class input on *both* axes and routes every threshold
decision through one type, because the alternative — each surface comparing raw
columns for itself — is what let overlays declare 76 columns on a 36-column screen
and let a phone in landscape keep desktop chrome it had no rows for.

`ui.rs` (layout choice) and `ui/widgets.rs` (overlay geometry) are upstream-owned:
a sync can take upstream's version of either region without a conflict and quietly
restore the width-only world.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-MOB-01 | Width classifies into tight / compact / regular at exact boundaries. | An off-by-one at a boundary sends a whole class of phone widths down the wrong layout path, and nothing reports it. | `size_class_width_boundaries` |
| TP-MOB-02 | Height classifies into short / regular at an exact boundary. | The landscape-phone case silently rejoins the desktop case, restoring the chrome that has no rows to spare. | `size_class_height_boundaries` |
| TP-MOB-03 | The tight ceiling follows the configured mobile threshold down, so the two can never cross. | A user who lowers `mobile_width_threshold` below the tight ceiling creates widths that are "too narrow for the mobile shell" — a state with no defined behavior. | `tight_never_exceeds_the_mobile_threshold` |
| TP-MOB-04 | A zero-width or zero-height viewport is not mobile, matching the predicate this replaced. | A degenerate frame starts taking the mobile path, changing behavior on an area no caller ever had to draw. | `zero_area_is_not_mobile` |
| TP-MOB-05 | Routing the shell decision through the size class returns exactly what the width-only predicate returned, for every width and threshold. | The shell silently flips for some viewport the migration never considered — the one regression a type-level refactor is most likely to hide. | `mobile_shell_matches_the_width_predicate_it_replaces` |
| TP-MOB-06 | Every viewport that already took the desktop shell keeps identical popup geometry. | Narrow-screen work moves desktop cells, so a change nobody asked for ships to every existing user. | `regular_viewport_popup_geometry_is_unchanged` |
| TP-MOB-07 | On a tight viewport a popup spans the full width instead of floating. | Four columns of margin on a 36-column screen is a tenth of the readable area, spent on a floating affordance that at that size no longer reads as floating. | `tight_viewport_popup_spans_the_full_width` |
| TP-MOB-08 | A compact viewport keeps one column of margin, not the desktop's two. | The popup either loses the panel reading entirely or keeps paying desktop margin on a screen that cannot afford it. | `compact_viewport_popup_keeps_one_column_of_margin` |
| TP-MOB-09 | A short viewport spends no rows on vertical margin, and being short does not make it narrow. | A landscape phone loses two of fourteen rows to margin, or — worse — is treated as narrow and has its width cut as well. | `short_viewport_popup_drops_the_vertical_margin` |
| TP-MOB-10 | A viewport too small for a bordered body still refuses to produce a rect. | Callers receive a rectangle nothing can be drawn into and paint a border with no interior. | `impossible_viewport_still_returns_none` |
| TP-MOB-11 | A narrow help body stacks each label under its shortcut instead of wrapping it. | A wrapped label continues at the left edge of the next line, under the key column, so the two-column alignment the layout exists for is destroyed exactly when the screen can least afford confusion. | `narrow_keybind_help_stacks_labels_under_their_keys` |
| TP-MOB-12 | No help row is wider than the body it was laid out for, at any width. | The overlay writes past its own border, and the row that does it changes with the user's keybinds, so it is never the one anybody tested. | `keybind_help_rows_never_exceed_the_body_width` |
| TP-MOB-13 | In a rendered narrow overlay, no body row starts flush against the border with a label tail. | This is the shape the wrapped layout produced on a phone: a row reading `mode` under a column of shortcuts, with nothing saying what it belongs to. | `narrow_keybind_help_never_starts_a_row_with_a_label_tail` |
| TP-MOB-14 | The search hint shortens to a phrasing that still names the key rather than being cut mid-word. | A hint cut to `…by command or short` spends the same columns saying less, and stops telling the reader which key opens the filter. | `keybind_help_search_hint_shortens_instead_of_truncating` |
| TP-MOB-15 | A wide help body keeps the single-line, key-aligned layout. | The stacked form doubles the height of the help on screens that never needed it, pushing most of the list below the fold. | `wide_keybind_help_keeps_the_single_line_layout` |
| TP-MOB-16 | The footer drops hints by rank and never drops the way out of the overlay. | The footer is cut mid-word, or fits by hiding `esc` — leaving a reader who opened the help by accident with no visible way back. | `narrow_keybind_help_footer_drops_hints_by_rank` |
| TP-MOB-17 | The footer never renders wider than its strip, in either state, at any width. | The hint strip overruns the modal border, and which state does it depends on whether the reader has focused the filter. | `keybind_help_footer_never_exceeds_its_strip` |
| TP-MOB-18 | The settings popup's height is budgeted against the width its body actually gets, not the width it declares. | The integrations footer wraps onto more rows than were reserved for it once the viewport clamps the popup narrower, and the bottom of the settings body is cut off — on exactly the screens least able to spare it. | `settings_height_is_budgeted_against_the_real_inner_width` |
| TP-MOB-19 | A viewport roomy enough to grant the declared width keeps the height it always had. | Two-pass sizing changes desktop settings for no reason. | `settings_height_is_unchanged_on_a_roomy_viewport` |
| TP-MOB-20 | The settings body never draws past the popup it was sized for, at any reachable viewport. | The overlay writes into rows below itself, leaving fragments over the surface behind it that nothing clears. | `settings_integrations_body_stays_inside_its_popup` |
| TP-MOB-21 | The settings hit geometry is sized by the same two passes the render uses. | Both sides derive the height independently; the moment that height depends on the granted width, a second derivation puts every click target on a row the render did not draw it on. | `settings_hit_targets_land_on_the_rows_the_render_drew` |
| TP-MOB-22 | A viewport too small for a modal draws a notice naming the overlay and its exit key, never a blank. | The mode stays active over a surface with no overlay on it: keystrokes go somewhere invisible, and nothing tells the reader which key gets them back. | `a_modal_too_small_to_draw_leaves_a_notice` |
| TP-MOB-23 | The too-small notice keeps the exit key at every width and never overruns its strip. | The one line whose entire job is "here is the way out" loses the way out, or writes past the frame. | `the_too_small_notice_always_names_the_exit_and_fits` |
| TP-MOB-24 | Drawing the notice never panics, down to a one-cell frame. | Resizing a terminal to a sliver crashes Herdr instead of degrading. | `the_too_small_notice_survives_a_one_cell_frame` |
| TP-MOB-25 | No overlay mode leaves a blank screen on a viewport too small to hold it. | This is the class, not the instance: every mode-opening overlay used to return silently below its minimum, and each new overlay would have inherited the same blank. | `no_overlay_mode_leaves_a_blank_screen_on_a_tiny_viewport` |
| TP-MOB-26 | Which shell a viewport gets is still decided by its width alone, at every size. | Height became an input to the layout; if it silently became an input to the *shell* too, some viewport would swap between the mobile and desktop shells on a resize nobody associated with the change. | `the_shell_choice_is_unchanged_for_every_viewport` |
| TP-MOB-27 | A wide but very short viewport — a phone held sideways — keeps the desktop shell. | Ninety columns fit a sidebar; the scarce resource there is rows, and swapping shells returns none of them while throwing away the workspace list. | `a_wide_short_viewport_keeps_the_desktop_shell` |
| TP-MOB-28 | A short viewport folds the sidebar to its status rail, and unfolds it when the rows return, without writing the collapse preference. | Either a landscape phone spends a quarter of its width on a sidebar it has no rows to fill, or the fold is written into the preference and the sidebar stays collapsed long after the terminal grew back. | `a_short_viewport_folds_the_sidebar_to_its_rail` |
| TP-MOB-29 | An explicit expansion outranks the short-viewport fold for the rest of the session. | The heuristic keeps arguing with someone who has already answered it, re-folding a sidebar they deliberately opened. | `an_explicit_expansion_outranks_the_short_viewport_fold` |
| TP-MOB-30 | A short viewport hides a tab strip showing a single entry, and restores it when the rows return. | One of fourteen rows goes to a strip that names the only tab there is. | `a_short_viewport_hides_a_single_entry_tab_strip` |
| TP-MOB-31 | A short viewport keeps a tab strip showing more than one entry. | Hiding it would make every other tab unreachable by mouse — the same trap `hide_tab_bar_when_single_tab` already avoids. | `a_short_viewport_keeps_a_multi_entry_tab_strip` |

## Mobile drawers

The single mobile switcher answered two different questions from one scrolling
list: "which project am I in" and "which tab of this project". The first is asked
when the context changes, the second constantly — and burying the frequent one
under the rare one meant reaching a tab required scrolling past every workspace
and every agent first. Two drawers, one per edge, split them.

`ui/mobile.rs`, `ui.rs` and `app/input/mouse.rs` are upstream-owned.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-MOB-32 | An open drawer covers three quarters of the width and leaves the rest showing. | A full-width panel loses both jobs the uncovered strip does: it is the target that closes the drawer, and the reminder that a session is running underneath. | `a_drawer_covers_three_quarters_and_leaves_the_rest` |
| TP-MOB-33 | The tabs drawer hangs off the right edge, the spaces drawer off the left. | Both panels arrive from the same side and the reader has to read a title to tell which question they are looking at, every time. | `the_tabs_drawer_hangs_off_the_right_edge` |
| TP-MOB-34 | A closed drawer projects no geometry and no rows. | Hit-testing and painting run against a panel that is not open, so taps on the terminal are swallowed by an invisible surface. | `a_closed_drawer_projects_no_geometry` |
| TP-MOB-35 | A drawer starts below the header and runs to the bottom. | The header disappears under the drawer, taking with it the toggle that closes it and the active-tab context. | `a_drawer_starts_below_the_header` |
| TP-MOB-36 | Every row the producer emits hit-tests back to its own target, at every document position it occupies. | Render, hit-testing and height derived the same layout independently — the file asked readers to keep them in step by hand. A cursor made it four. Any drift sends a tap to the wrong row. | `every_drawer_row_hit_tests_back_to_itself` |
| TP-MOB-37 | The scroll height is the sum of the rows the producer emits. | The drawer scrolls past its own end or refuses to reach it, and which one depends on how many worktrees the person has. | `the_drawer_height_is_the_sum_of_its_rows` |
| TP-MOB-38 | REVERSED 2026-08-02 by TP-MOB-87. Tight viewports used to compress each entry to one row to shorten the document. | One-line entries put three or four rows under a single fingertip — the measured root of the "my finger touches everything" report. Reachability of an overflowing document is the pinned footer's job (TP-MOB-77/80), not thin rows'. | *(test removed; superseded by `tappable_rows_meet_the_density_floor`)* |
| TP-MOB-39 | A drawer scrolls only when its content overflows. | Either a short list scrolls under the reader for no reason, or a long one cannot reach its end. | `a_drawer_scrolls_only_when_its_content_overflows` |
| TP-MOB-40 | Each header button opens its own drawer, and opening one closes the other. | Two panels claim the same screen, and which one wins depends on which code path ran last. | `each_header_button_opens_its_own_drawer_and_closes_the_other` |
| TP-MOB-41 | Pressing an open drawer's own button closes it. | A control that opens but cannot close is half a toggle, and on a phone the button is the nearest thing to hand. | `pressing_an_open_drawers_own_button_closes_it` |
| TP-MOB-42 | The active-tab strip dispatches the same action as the button beside it. | The strip becomes decoration, and a tap that misses the three-column button does nothing at all. | `the_tab_strip_opens_the_same_drawer_as_the_button_beside_it` |
| TP-MOB-43 | Tapping the uncovered strip closes the drawer and does not reach the terminal. | Dismissing a panel focuses a pane the reader never meant to touch. | `tapping_the_scrim_closes_the_drawer_without_reaching_the_terminal` |
| TP-MOB-44 | A tap inside the drawer that lands on no row leaves it open. | Every near-miss in a list dismisses the thing being read. | `tapping_empty_space_inside_the_drawer_leaves_it_open` |
| TP-MOB-45 | The header keeps two separate, reachable targets however narrow the viewport. | The buttons overlap and one of the two intents becomes unreachable, with nothing saying which. | `the_header_targets_never_overlap_however_narrow_the_viewport` |
| TP-MOB-46 | The drawer cursor opens on the row the reader is already in — the active workspace, the active tab. | The first arrow key moves relative to the top of a list nobody asked to be at the top of, so reaching the neighbouring workspace takes as many keys as there are workspaces before it. | `the_drawer_cursor_opens_on_the_current_row` |
| TP-MOB-47 | Arrow keys stop only on rows something happens on, and reach every section including the menu. | A cursor that stops on section headings spends keypresses on rows that do nothing, on a surface where every keypress costs a thumb movement. | `the_drawer_cursor_skips_rows_that_are_not_targets` |
| TP-MOB-48 | The cursor reaches agents from the keyboard. | This is the row class the old switcher could only reach by tap — and a tap is the unreliable input on the clients this surface exists for, so the drawer's stated primary job was unavailable there. | `the_drawer_cursor_reaches_agents_from_the_keyboard` |
| TP-MOB-49 | Enter does what tapping the cursor's row would do, through the same targets. | A keystroke and a tap drift into meaning different things, and which one a reader gets depends on whether their client forwards mouse events. | `enter_activates_the_row_the_cursor_is_on` |
| TP-MOB-50 | Tab swaps between the two drawers. | Reaching the other drawer means going back to the header — the one part of a phone screen a thumb has to stretch for. | `tab_swaps_between_the_two_drawers` |
| TP-MOB-51 | The cursor clamps at both ends rather than wrapping. | On a list whose length changes with the number of worktrees, a held arrow key loops silently and the reader loses their place. | `the_drawer_cursor_clamps_at_both_ends` |
| TP-MOB-52 | Turning select text on releases mouse capture and turning it off restores it. | With mouse reporting on, an iOS terminal suppresses its own press-and-hold selection entirely: the drag goes to the application and the selection handles never appear, so copying text on a phone is impossible. | `select_text_releases_mouse_capture_and_restores_it` |
| TP-MOB-53 | The toggle restores the capture setting it found, not a hardcoded default. | A reader who runs with `mouse_capture = false` has it silently switched on by using a feature that has nothing to do with it. | `select_text_restores_the_setting_it_found` |
| TP-MOB-54 | The spaces drawer carries the toggle on a row the keyboard cursor can reach. | While capture is released a tap reaches nothing, so a toggle reachable only by tap is a one-way door out of Herdr's own interface. | `the_spaces_drawer_offers_a_reachable_select_text_row` |
| TP-MOB-55 | While select text is on, the header says so and names the way back. | This is the one mode that changes whether taps do anything at all; without an indicator the reader concludes Herdr has stopped responding. | `the_header_says_when_select_text_is_on` |

## Mobile scrolling

A touch client reports a swipe as a wheel event. On a desktop that wheel belongs
to the program in the pane, and handing it over is what makes scrolling work
inside its own lists and viewers. On a phone the same handover is a dead end: an
agent that asked for mouse reporting usually ignores the wheel, and the reader
has no second way to reach scrollback — no wheel, no keyboard shortcut within
reach, and a scrollbar too thin for a finger. The phone shell keeps the vertical
wheel over pane content for its own viewport.

| ID | Behaviour | Why it exists | Test |
| --- | --- | --- | --- |
| TP-MOB-56 | In the mobile shell a vertical wheel over pane content scrolls Herdr's viewport instead of being reported to the pane — while the host has scrollback to give (amended 2026-08-03 by TP-MOB-97). | A swipe is the only scroll gesture a phone has; spending it on an agent that discards it leaves the content area looking frozen. | `mobile_shell_vertical_wheel_scrolls_the_pane_instead_of_reporting_it` |
| TP-MOB-57 | By default a desktop layout's vertical wheel still reports to a mouse-reporting pane and does not move Herdr's viewport; the scope is a setting since TP-MOB-101, and this row pins its default. | The override is a phone accommodation; applying it everywhere *by default* would break scrolling inside every mouse-aware program Herdr hosts. | `desktop_shell_vertical_wheel_still_reports_to_the_pane` |
| TP-MOB-58 | The header buttons are at least five columns wide and both are the same width. | They are the only tap targets the phone shell always shows and they sit in the two corners a thumb reaches least accurately; three columns is roughly half the 44pt floor Apple asks for. | `the_header_buttons_are_wide_enough_for_a_thumb` |
| TP-MOB-59 | A viewport too narrow for two full buttons shrinks them evenly instead of overlapping them or starving the right one. | Spending all the narrowing on the right button leaves it a sliver on a merely narrow screen, and overlapping targets make one intent unreachable without saying which. | `the_header_buttons_shrink_before_they_overlap` |
| TP-MOB-60 | The spaces drawer carries every level of the workspace tree: the repository header, the checkouts under it, and their remembered chats. | It used to keep only the checkout rows, so worktrees from different repositories landed in one flat list and a remembered chat could not be reached from a phone at all. | `the_spaces_drawer_carries_every_level_of_the_tree`, `the_mobile_drawer_sees_chat_rows`, `the_mobile_drawer_sees_the_group_header` |
| TP-MOB-61 | The three levels carry three different depths. | The row used to carry a single `indented` flag, so a repository, a checkout under it and that checkout's chat all drew at the same offset — the reader could not tell them apart. | `the_three_levels_carry_three_different_depths` |
| TP-MOB-62 | Folding a repository group hides its linked worktrees on the phone too, and the header stays so it can be opened again. | The flat switcher had no way to fold a group so mobile forced every one open; with sixteen workspaces that is a list nobody can scroll. | `a_collapsed_group_hides_its_members_from_the_drawer`, `the_spaces_drawer_follows_grouped_worktree_order` |
| TP-MOB-63 | The keyboard cursor only stops on rows that are drawn, and never on an inert note. | A cursor on a hidden row is the tree form of a mode with no surface: the arrow key moves, nothing on screen does, and the reader loses their place. | `the_drawer_cursor_only_stops_on_rows_that_are_drawn` |
| TP-MOB-64 | The indent budget leaves the label its last columns however narrow the panel. | Ratatui does not clip a line that overruns its rect, so indenting without a floor pushes a name past the panel edge and corrupts the row. | `indenting_never_costs_the_label_its_last_columns` |
| TP-MOB-65 | A tap reported one column or row past the last cell still reaches the target under that cell. | A touch client clamps an edge tap to the screen size rather than one less, so on a 76-column phone edge taps arrive at column 76 — outside every rect, and the rightmost column is where one of the two header buttons lives. Measured live: 3 of 37 taps. | `a_tap_past_the_last_column_still_reaches_the_button_there` |
| TP-MOB-66 | The header buttons accept a tap one row below the header; the strip between them does not. | The header is two rows tall and a thumb aiming at a five-by-two target routinely lands just under it, where the tap used to reach the terminal and do nothing — which reads as a broken button. Measured live: 3 of 37 taps. The strip spans most of the width, so a row of reach there would swallow the terminal's top row instead. | `the_header_buttons_accept_a_tap_just_below_them` |
| TP-MOB-67 | The workspace being worked in shows the chats it remembers, with nothing to press first. | The only thing that ever opened a chat drawer was a single cell on the desktop sidebar's workspace card, which the phone shell does not draw — so the chats sat in the ledger unreachable. A cell that small is not a phone target either: a five-by-two button already lost one tap in six. | `the_workspace_you_are_in_shows_its_remembered_chats` |
| TP-MOB-68 | Only that workspace. The others keep their chats folded. | Opening all sixteen at once rebuilds the wall the drawer exists to avoid. | `the_other_workspaces_keep_their_chats_folded` |
| TP-MOB-69 | Activating the workspace row you are already on folds its chats and leaves the drawer open; any other row switches and closes. Every row keeps the same target — the branch lives in the activation. | The row you are on cannot mean "switch to this". Splitting the *target* instead broke both consumers that identify a workspace row by it: the doc range the drawer scrolls to, and the keyboard cursor's selection sync. | `activating_the_row_you_are_on_folds_its_chats_rather_than_switching`, `tapping_the_active_workspace_folds_its_chats_without_closing_the_drawer` |
| TP-MOB-70 | Only the active workspace draws detail text on its second row; every workspace row spends the same touch height (amended 2026-08-02 by TP-MOB-87 — the height half of the old trade is reversed, the content half survives). | The detail answers "what state is this branch in", asked about one branch rather than sixteen; the other branches keep their extra rows as breathing room, so a fingertip finds the same shape on every row while the text is what varies. | `only_the_active_workspace_spends_a_second_row_on_detail` |
| TP-MOB-71 | REVERSED 2026-08-02 by TP-MOB-87: sixteen touch-height workspaces overflow the drawer by design; what holds instead is that the escape hatch stays pinned above the overflow and scrolling reaches every unpinned row. | A list that fits but cannot be tapped is worth less than one that scrolls; the original worry — `select text` below the fold — was solved by pinning it (TP-MOB-76/77), which made fitting unnecessary. | `the_drawer_fits_the_reader_s_real_workspace_count` |
| TP-MOB-72 | The keyboard cursor stops on the chat rows. | An iOS terminal does not reliably deliver a tap, so a row only a finger can reach is only half reachable. | `the_keyboard_cursor_stops_on_the_chat_rows` |
| TP-MOB-74 | A narrow display keeps the phone's wheel rule while a wide one draws. | The rule reads `view.layout`, and the view is one structure the last render leaves behind. Without the input path recomputing geometry for the display that sent the event, a swipe from the phone would be decided against the desktop's width and handed to the agent, which discards it. | `a_narrow_display_keeps_its_wheel_rule_while_a_wide_one_draws` |
| TP-MOB-75 | The phone drawer, its cursor, its chat fold and select-text belong to the display they were opened on. | Shared, a phone attached beside a desktop opened and closed the desktop's drawer with its own. They are `owned` rather than `broadcast` because a broadcast surface promotes what one display changed into what every later display adopts — right for a mode, wrong for a drawer: a display that has just attached has not opened one. | `a_drawer_belongs_to_the_display_it_was_opened_on` |
| TP-MOB-76 | `select text` and the drawer's primary action sit in a footer band whose screen position scrolling cannot move, and hit-testing maps that band by screen position alone. | An escape hatch whose position depends on how far the reader has scrolled is not an escape hatch — `select text` is the way back out of a mode where taps reach nothing at all. The reference mobile app pins its primary action for the same reason. | `the_footer_keeps_its_targets_on_screen_at_max_scroll` |
| TP-MOB-77 | The create action leads neither drawer's scroll document; it lives in the pinned footer, styled as the drawer's one primary action. | At the top it was the first thing a thumb had to scroll past on every open; the bottom is the zone a thumb rests over. | `the_primary_action_lives_in_the_footer_not_the_scroll` |
| TP-MOB-78 | Scroll range covers the scrollable content only, never the pinned tail. | Scroll that "reveals" rows already on screen runs the list past its own end into blank rows. | `max_scroll_excludes_the_pinned_tail` |
| TP-MOB-79 | Stepping the keyboard cursor onto the footer does not move the scroll. | The footer is always on screen; a follow that treated its document position as something to reveal would jump the list to the bottom the moment the cursor entered the band, and the reader loses the row they were just on. | `stepping_onto_the_footer_needs_no_scroll` |
| TP-MOB-80 | A short drawer body clips the footer before it starves the list, and the two never overlap. | The list is what the drawer exists for; the footer is a convenience on top of it, and two bands drawing over each other is the one outcome worse than either being short. | `a_short_drawer_body_never_overlaps_footer_and_list` |
| TP-MOB-81 | A blank row precedes each section title at every width (amended 2026-08-02: the tight-width exception fell with TP-MOB-87). | The separation is most of what reads as composed about the reference app's drawer — a heading jammed against the row above reads as one more list item. Once the density floor gave every entry breathing room regardless of width, a spacer-less tight drawer would have been the one cramped row style left. | `sections_breathe_when_the_width_affords_it` |
| TP-MOB-82 | Every drawer promise holds at the widths a large font leaves: 50, 38 and 32 columns keep the touch floor, a hittable footer, reachable chats and a bounded indent (amended 2026-08-02: "one-line rows" became the floor when TP-MOB-87 landed). | The reader's answer to small text is the client's font setting, and growing it shrinks the columns; anything that breaks under that shrink punishes the reader for making the screen legible — and a grown font is exactly when touch targets matter most. | `the_drawer_keeps_its_promises_at_large_font_widths` |
| TP-MOB-83 | The phone shell never animates a working indicator: the header and every agent row in the drawer show steady state colours — red blocked, yellow working, teal done, green idle. | Every distinct frame is a redraw on a GPU-less phone terminal and a reason to keep the radio awake, eight times a second for as long as an agent works. Identical frames are deduplicated before they reach the client, so a still indicator costs nothing; the desktop keeps its spinner because its terminal renders for free and its link is a wire. | `the_mobile_header_holds_still_while_an_agent_works` |
| TP-MOB-84 | A workspace row carries three tap zones: a chat disclosure at its head, the row itself, and a `+` at its tail that starts a chat in that branch. Each zone is at least three cells and each is drawn where it taps. | Looking at a branch's history is not travelling to it, and starting a chat there is; a measured one-in-six of real taps missed even a five-cell target, so anything narrower is decoration. A control that does not look like one is a dead spot. | `a_branch_row_carries_three_zones` |
| TP-MOB-85 | A chat row ends with when its last message landed: now, 5m, 3h, 2d. | Recency is what a history is scanned for; titles without ages answer \"what did I do\" but never \"where was I last\". | `chat_age_reads_compactly` |
| TP-MOB-86 | Each client attach logs whether the host terminal reports its pixel size, and treats any zero winsize field as "not reported". | The mobile shell's touch-target math runs on a derived cell↔pt bridge; the only place a real measurement can come from is the terminal the phone sits in, and most SSH stacks forward ws_xpixel as zero. Without this line the derived numbers can never be confirmed or corrected. | `host_cell_px_reads_only_a_fully_reported_winsize` |
| TP-MOB-87 | Every tappable drawer row spends at least three document rows in a regular-height viewport and two in a short one — never one. Rows without a target (titles, notes, spacers) stay thin. | A terminal row is 11–13pt on this phone against a ≈44pt fingertip pad: a one-line target puts three or four rows under one touch, which is the measured root of "my finger touches everything at once". Width never lowers the floor, because a grown font is when the reader needs targets most; a short viewport drops the breathing row, never the floor. | `tappable_rows_meet_the_density_floor` |
| TP-MOB-88 | The pinned band is touch-sized too: the create action spends four rows, `select text` three, and the panel's very last row answers no tap. | The create action is the drawer's one primary commitment and lives where the thumb lands without looking. The keyboard's arrival changes the row count under that thumb (measured 35↔63 rows), so whatever sits on the final row is exactly what slides out from under a landing finger — a guard row of nothing is the cheapest possible miss. | `the_pinned_band_is_touch_sized_and_clears_the_last_row` |
| TP-MOB-89 | The mobile header spends four rows in a regular-height viewport (two in a short one) and its buttons are nine columns wide across its full height, plus the one-row reach below. | A terminal cell is anisotropic — ≈5pt wide by ≈12pt tall on this phone — so a 44pt square is nine columns by four rows, not a square of cells; the old 5×2 button was 26×23pt, half the floor on both axes, in the two corners a thumb reaches least accurately. The strip absorbs the narrowing because missing a button is a failed action while missing the strip is only a missed shortcut. | `the_header_meets_the_touch_floor` |
| TP-MOB-90 | No readable text in the mobile shell is painted `overlay0`; words that must recede wear `overlay1` with DIM, and `overlay0` keeps only the wordless work — tree connectors, separators, ellipses. | Measured against WCAG 2.1: `overlay0` on the panel is 3.59:1, under the 4.5:1 AA floor, and worse on raised surfaces; a phone is read in sunlight. `overlay1` reads at 4.75:1 and DIM is free — every cell already carries the modifier field. | `readable_drawer_text_clears_the_contrast_floor` |
| TP-MOB-91 | The left drawer opens with a two-row segment band — spaces · projects — and its projects segment walks the desktop Projects tree: a foldable header per pinned project with a trailing `+` that starts a fresh chat there, a touch-height row per remembered session that resumes it, the surplus as "… N older". The segment is this display's own (`sidebar_tab`); resuming travels and closes the drawer, switching segments and folding do not. | The desktop rail already answers "what did I work on, where" — a phone without the projects tree simply could not reach remembered chats outside its open workspaces. Per-display, because one display switching rails once moved every display with it; and the tap path and the keyboard cursor share one application point so the two roads cannot drift. | `the_drawer_walks_the_projects_tree_behind_its_segment_band`, `projects_activations_travel_and_segment_switches_do_not` |
| TP-MOB-92 | The drawer draws its structure: the primary action sits in a rounded accent pill and the active segment is a tab-shaped box open toward the list it selects. Rounded corners (`╭ ╮ ╰ ╯`) joined the border vocabulary by the reader's explicit decision. | A button that is only a coloured word is implied, not drawn; the reader asked for visible regional borders. The measured cost answer: static cells are free in the frame diff, so a border costs its first paint and nothing in daily use — the one-thin-family rule was an aesthetic default, and aesthetics here are the reader's call. | `the_primary_action_wears_a_rounded_frame` |
| TP-MOB-93 | The segment band carries all three rail segments — spaces, projects, files — in equal thirds; files is a surface, not a list: choosing it opens the file browser in the terminal area and closes the drawer, and a drawer reopened on the Files tracker shows the spaces tree. | The desktop rail has three segments and Files was the missing third; it never was a rail list even there, so its mobile meaning is "go to the browser", which is travelling and follows the travelling rule. The spaces fallback is the desktop's own: a drawer with no list is a dead end. | `the_segment_band_offers_files_and_files_is_a_surface` |
| TP-MOB-94 | A workspace row's tail carries `⋯` beside its `+`: the touch road to the same menu the desktop opens with a right click — rename, close, worktree operations. The menu floats over the open drawer, anchored at the row, and the zone folds first on panels too narrow to keep the label's floor. | Without a touch road those actions do not exist on a phone at all. Choosing an action about a row is not leaving the list, so the drawer stays; the kind derivation mirrors the desktop's so the two surfaces cannot offer different truths about the same workspace. | `a_branch_row_offers_its_menu_from_the_tail` |
| TP-SUR-SHELL-01 | Every floating panel shell — context menus, popups, dialogs — wears rounded corners; the panes and lists behind them keep the light family. | What floats above the surface is drawn as floating: the reference file managers keep exactly this split (yazi rounds its overlays — confirm, notify, pick — and leaves its panes plain), and the reader asked for the rounded language explicitly. One shared shell means one voice for every popup, and static border cells are free in the frame diff. | `floating_shells_wear_rounded_corners` |
| TP-MOB-95 | The drawer's text reads in four layers on one font size: the active branch name is the only bold branch name, chat titles wear `subtext0`, details and dim notes wear `overlay1`, decorations keep `overlay0`. | A terminal has no type scale — the client's font setting is the only size there is — so hierarchy is carried entirely by weight and colour. When every name is bold, none is; and a chat title is tappable content, not commentary, so it sits a colour layer above the detail lines. | `drawer_text_reads_in_layers` |
| TP-MOB-96 | An open mobile drawer owns the mouse: while it is up, the background pre-branches — the file manager surface, dock, divider and URL clicks — stay inert, exactly as they do under a blocking overlay. | Paint order and input order must not disagree about what is on top. Reported live: with Files open the drawer drew above the browser but the browser ate its taps, so every segment and row fell through dead and there was no touch road out of Files. | `the_drawer_stays_responsive_over_the_file_browser` |
| TP-MOB-97 | The phone shell only claims the vertical wheel when it can honour it: over a pane with no scrollback — an alternate-screen program, the shape of every agent TUI — the swipe reaches the program instead. | Reported live as "scroll sometimes does not work": the host tried to move a scrollback that alternate screens do not have, and the only gesture a phone has was spent on nothing. A swipe must always move something, and whoever can move it owns it. | `mobile_wheel_reaches_an_alt_screen_program_the_host_cannot_scroll_for` |
| TP-MOB-101 | `ui.mouse_wheel_host_scroll` decides which surfaces may claim the vertical wheel from a mouse-reporting pane: `mobile` (default, the phone shell only — TP-MOB-56/57), `scrollable` (every surface), `never` (no surface). No mode overrides the scrollback guard: a pane the host cannot scroll for keeps its wheel in all three (TP-MOB-97). | Reported live on a desktop as "scroll does not work in the content area": an agent asks for mouse reporting and then discards the wheel, so the gesture lands nowhere and the pane looks frozen. The usual escape does not exist here — the host terminal captures shift+wheel for itself before Herdr sees it (measured: ghostty `mouse-shift-capture = false`), and Herdr runs on the alternate screen where the host's own scrollback is empty. Without this setting the only ways out are the pane edge, the scrollbar and copy mode, none of which a reader finds by reaching for the wheel. | `scrollable_mode_gives_the_desktop_wheel_to_host_scrollback`, `scrollable_mode_still_leaves_an_alt_screen_program_its_wheel`, `never_mode_takes_the_wheel_back_from_the_phone_shell`, `scrollable_mode_does_not_claim_the_horizontal_wheel`, `mouse_wheel_host_scroll_parses_and_defaults_to_mobile` |
| TP-GFX-RESET-01 | Her client bağlantısı koşulsuz kitty delete-all bariyeriyle başlar (`a=d,d=A,q=2`) ve grafik yükü düşürülen kare (oversized drop / text-only serialize fallback) bariyeri yeniden kurar. `clear_bytes` yalnız cache'in bildiği id'leri silebilir; kayıp silme karesi / sunucu restartı / başka programın kalıntısı dış terminalde KİMSENİN hatırlamadığı id bırakır — bariyer onlara ulaşan tek silmedir. | Kaybolursa: dış terminal yetim görüntüleri sonsuza dek gösterir (2026-08-25 canlı vaka: tb kapanış-silmesi kayboldu, kullanıcı ekranında saatlerce görsel kaldı; hiçbir pane-içi silme ulaşamadı). | `a_fresh_client_first_frame_opens_with_a_delete_all_barrier` · `a_rearmed_barrier_fires_again_on_the_next_frame` · `reset_barrier_wipes_unknown_images_too` (iki drop-site tek-satır set'leri aynı marker'ı taşır; mekanizma ikinci testle çivili) |
| TP-GFX-STREAM-02 | Sahipsiz-stream süpürücüsünün doğruluk kaynağı GERÇEK slot durumudur: bir owner, herhangi bir pane-graphics slotu onu adlandırıyor ve gate'i açıkken CANLIDIR; süpürücü yalnız slot'suz/gate'i kapalı (hayalet) kayıtları iptal eder. | Kaybolursa: süpürücü hiç doldurulmayan bir kaynağa bağlanırsa her `pane.graphics.stream` kaydolduğu ilk tick'te kesilir (2026-08-27 canlı vaka: saniyede 1, 898+ stream_closed; her tab'da donmuş tarayıcı karesi; grafik talebi düşünce piksel-fare geri çekildi — upstream-sync onarımı 8074ed8b tam bunu yapmıştı). | `a_live_stream_owner_is_recognised_by_the_cancel_sweep` |
| TP-GFX-CONVERGE-01 | Bir client'a gönderilecek kare "metin aynı" diye atlansa bile grafik baytları DÜŞÜRÜLMEZ: skip dalı grafik taşıyorsa `ServerMessage::Graphics` olarak gönderilir ve `graphics_cache` YALNIZ gönderim başarılıysa commit edilir (Full→defer, oversized→reset bariyeri). Böylece her client, hızlı ardışık pane-resize'lardan sonra nihai geometriye yakınsar. | Kaybolursa: çok-client oturumda pane daraltılınca eski geniş placement bir client'ın ekranında SONSUZA DEK kalır (2026-08-27 canlı vaka: kullanıcının Mac kitty'sinde kapatılmış videonun kalıntı şeridi; lab 3/3 deterministik repro). | `every_client_converges_to_the_final_pane_geometry_after_rapid_resizes` · `narrowing_the_view_reclips_or_deletes_a_stale_pane_graphics_placement` |

## Pointer presses on graphics panes

A pane that paints through `pane.graphics` is usually driven by a program
that never enabled terminal mouse reporting — terminal-browser's pane mode is
the live case (2026-08-26): every click fell through to the UI and vanished.
Herdr does not force mouse bytes on a program that did not ask for them; it
queues the press and publishes it as an API event for panes that own a
graphics layer, so an out-of-band bridge can deliver it over CDP. Programs
that did enable a mouse mode keep the press exclusively.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-INP-MOUSE-01 | A mouse press or release on a pane whose program never enabled terminal mouse reporting is queued by the input layer and published as a `pane.pointer` event when the pane is painting graphics — either an API `pane.graphics` layer or terminal-native kitty graphics transmitted by the pane's own program (the browser case), detected by generation rather than a live placement so a not-yet-sized or mid-repaint frame still counts; a program that did enable a mouse mode keeps the press exclusively and no event is emitted; a plain text pane stays silent. The server loop drains the queue every pass, and a queued press is published even if the viewer's active workspace moved before the drain (the pane is found where it lives). | A browser or viewer painting into a pane is click-dead again: the press falls through to the UI and no bridge can deliver it (the 2026-08-26 live case — an API-slot-only gate passed every unit test yet dropped every real terminal-browser click). If the exclusivity half is lost instead, an SGR-mode program receives every press twice. If the loop call is lost, presses queue forever and every unit test stays green. | `an_unclaimed_click_on_a_graphics_pane_is_published` · `an_unclaimed_click_on_a_terminal_graphics_pane_is_published` · `a_program_that_asked_for_the_mouse_keeps_it_exclusively` · `a_plain_text_pane_does_not_publish_pointer_events` · `a_queued_press_is_published_even_when_its_workspace_is_no_longer_active` · `the_server_loop_publishes_queued_pointer_presses` |
| TP-INP-MOUSE-02 | `pane.pointer` is a subscription type of `events.subscribe` (`{"type":"pane.pointer"}`); the subscription starts at the moment it is made rather than at the hub's first retained event; and the event's wire shape is fixed: pushed lines carry event name `pane_pointer` (generic event names are the kind's snake_case name on the wire; the dot name is the subscription type), data tag `pane_pointer`, kind `down` or `up`, button 0, 1 or 2, cell `column` and `row`, pixel fields omitted when unknown. | A bridge cannot subscribe at all (the parser refuses unknown types); or on attach it receives every press the hub still retains and clicks the page with stale gestures; or a respelled tag or kind silently stops every bridge that parses the stream. | `pane_pointer_subscription_parses_from_its_dot_name` · `a_pointer_subscription_starts_at_now_and_never_replays_old_presses` · `pane_pointer_event_serializes_its_wire_shape` |
| TP-INP-MOUSE-04 | A pane program that enabled SGR pixel mouse mode (DECSET 1016) receives pixel coordinates even when the client reported a cell: the cell is converted to its pixel centre (1-based, matching the client's own pixel producer) for button, wheel and motion through the one shared encoder path. The cell/pixel wire shapes are identical, so no downgrade-to-cell report is ever sent while the terminal knows its pixel geometry; a terminal without pixel geometry keeps the old cell-shaped report rather than inventing pixels. Ordinary SGR (1006-only) panes keep cell numbers untouched. | Every click, wheel and motion in an embedded browser lands in its top-left pixel band again (the 2026-08-27 case: terminal-browser panes were click-dead locally and remotely because cell numbers were read as pixels). If the fallback half is lost, a pane without pixel geometry gets fabricated coordinates. | `sgr_pixel_pane_receives_pixel_coordinates_for_cell_positions` · `sgr_pixel_pane_receives_pixel_coordinates_for_wheel` · `sgr_cell_pane_keeps_cell_coordinates` · `ghostty_mouse_sgr_pixels_preserves_exact_and_converts_cell_input` · `ghostty_mouse_sgr_pixels_without_geometry_still_downgrades_cell_input` · `ordinary_cell_mouse_converts_pixel_mode_to_cell_centre_pixels` |
| TP-INP-MOUSE-05 | A client advertises its sub-cell (DECSET 1016) mouse capability in `Hello` independently of its launch mode, so a relayed transport (SSH, tmux, screen) that refuses whole-frame direct graphics still reports pixel positions; and the server asks for that mode when the focused pane paints graphics through **either** path — an API `pane.graphics` layer or terminal-native kitty transmissions from the pane's own program. A client without the capability is never asked, and a plain text pane never triggers the request. | Every remote session silently degrades to cell-quantised clicks: positions snap to the cell grid, so any target shorter than one cell falls between two grid points and becomes unreachable no matter where the user clicks (the 2026-08-27 live case — a 33 px form input under a 48.8 px cell, buttons worked and the form did not). If the two-path half is lost, embedded browsers regress the same way because they never register an API layer. | `ssh_keeps_the_pixel_mouse_while_direct_graphics_stays_off` · `an_unsupported_terminal_reports_no_pixel_mouse` · `a_pipe_reports_no_pixel_mouse` · `terminal_native_kitty_graphics_demands_the_pixel_mouse` · `a_plain_text_pane_does_not_demand_the_pixel_mouse` · `a_client_without_the_pixel_mouse_capability_is_never_asked_for_it` · `pixel_mouse_activation_requires_graphics_demand_not_direct_transport` |
| TP-INP-MOUSE-06 | Mouse modes seeded during a live handoff flow through the terminal stream as DECSET sequences, so ghostty's derived mouse flags — the only state its mouse encoder reads — agree with the mode bitset that wheel routing reads. A pane whose program never asked for the mouse stays silent after a handoff. | The mode bitset alone satisfies `mode_get` and wheel routing, so every layer *claims* the mouse works while the encoder returns empty for every wheel and click — the handed-off pane is mouse-dead until its program re-sends DECSET (the 2026-08-28 live case: 317 empty wheel encodes in the browser pane after a clientless live-handoff, clicks and trackpad dead). | `handoff_seeded_mouse_modes_drive_the_wheel_encoder` · `handoff_seeded_mouse_modes_drive_the_button_encoder` · `handoff_seed_with_mouse_off_keeps_the_encoder_silent` |
