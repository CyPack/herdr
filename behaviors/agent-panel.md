# Agent panel — active-row highlight and the close road

The lower sidebar panel answers "which agent am I on" at a glance. The active
row speaks the active tab's visual language — accent background with contrast
text — so the same question reads the same way on both surfaces, and the
passive rows give up their bold so the eye lands on the active one. Chosen by
the user on 2026-07-29 (accent-row model + passive dimming); design rationale:
`.local/prd/2026-07-29-flash-and-agent-panel-PRD.md`.

| ID | Behavior | What breaks if it is lost | Verified by |
|---|---|---|---|
| TP-AGPANEL-01 | The active agent's card (all of its rows, contiguous) wears the accent background with contrast text; no passive card does | The panel stops answering "which agent am I on" — the complaint this family exists for: active and passive rows differed only by a faint background | `the_active_agent_row_wears_the_accent_background_and_the_rest_stay_muted` |
| TP-AGPANEL-02 | A passive agent's name is drawn without bold | Every row shouts at the same volume, so the active card's emphasis carries no information | `the_active_agent_row_wears_the_accent_background_and_the_rest_stay_muted` |
| TP-AGPANEL-03 | A right-click on an agents-panel row opens that row's own menu carrying its (workspace, tab, pane, and the chat identity when the ledger knows it — TP-AGPANEL-28); the panel's empty space opens nothing, and the row is matched before the per-tab roads because the panel is shared chrome on every sidebar tab | The press falls through to the workspace list's row resolver, which answers for a different section — the panel would manage a workspace the user never pointed at, or the Projects tab would swallow the press entirely | `right_click_on_an_agent_row_opens_its_menu_and_the_panel_gap_stays_inert` · `the_agent_row_menu_offers_exactly_the_close_verb` |
| TP-AGPANEL-04 | "Close agent" closes the row's OWN pane through the pane close road (graceful close with its confirmation gate intact, never a kill), not whichever pane is focused | Closing from the panel kills the wrong agent — the focused one — or bypasses the worktree-group confirmation the pane road owns | `closing_an_agent_row_closes_that_row_pane_not_the_focused_one` |
| TP-AGPANEL-05 | A chat row offers "Close agent" only while that chat still has a running tab behind it, and offers it last | A drawer full of finished transcripts grows buttons that cannot act, and an irreversible verb sits above reversible ones | `a_chat_row_offers_close_only_while_something_is_running` |
| TP-AGPANEL-06 | The chat road resolves the session's tab at pick time, not at menu-open time; a session that no longer runs closes nothing | A menu left open while the agent exits fires a close at a tab index something else has moved into — a bystander dies | `closing_a_chat_agent_targets_the_session_tab_and_a_stale_menu_is_inert` |
| TP-AGPANEL-07 | The closed-agent ledger remembers ghosts newest first | "Recently closed" is an ordering claim; without it the words on the panel lie | `a_closed_agent_is_remembered_newest_first` |
| TP-AGPANEL-08 | Closing an agent that is already remembered refreshes its row (newest position, dormant again) instead of adding a twin | Two ghosts with one identity leave the user guessing which of them revives — and the answer is neither, reliably | `closing_the_same_agent_again_refreshes_the_row_instead_of_duplicating` |
| TP-AGPANEL-09 | The ledger is a fixed-capacity ring (8); the oldest row is evicted | Closing agents is a daily gesture by the hundreds; an unbounded list is unbounded memory wearing a feature's name | `the_ledger_evicts_its_oldest_beyond_capacity` |
| TP-AGPANEL-10 | Revival is a one-way claim (`Dormant → Reviving`), taken atomically before any spawn; a second attempt while one is in flight is inert | Two fast clicks both win and two tabs open — the exact spam the user named. A debounce only delays the same failure | `a_second_revival_attempt_while_one_is_in_flight_is_inert` |
| TP-AGPANEL-11 | A landed revival leaves the graveyard; a failed one returns to rest, clickable again | A row bricked in `Reviving` by one failure is inert to every later click for no reason the user can see | `a_finished_revival_leaves_and_a_failed_one_rests_again` |
| TP-AGPANEL-12 | The pane-close API road births the ghost BEFORE removal, while the terminal, the tab and the resume key are all still alive | Deriving the record afterwards is how #46 ended up spawning in `$HOME` — the state it needs is gone | `closing_an_agent_pane_leaves_a_ghost_in_the_ledger` |
| TP-AGPANEL-13 | A plain shell pane leaves no ghost | A graveyard of every closed shell buries the agents it exists to remember | `closing_a_plain_shell_pane_leaves_no_ghost` |
| TP-AGPANEL-14 | An agent whose process exits by itself becomes a ghost too (the `agent_released` road) | Only user-closed agents would be remembered; a crashed agent vanishes silently and the user asks where it went | `an_agent_that_exits_by_itself_becomes_a_ghost` |
| TP-AGPANEL-15 | The session snapshot carries the closed agents, newest first, with each row's in-flight revival state | The graveyard would be a TUI ornament; the PWA and every other client would see a different truth | `the_session_snapshot_carries_closed_agents_newest_first` |
| TP-AGPANEL-16 | An empty graveyard leaves no trace on the wire (`default` + skip-if-empty) | Old clients choke on new servers, or the field's absence reads as an error | `an_empty_graveyard_is_absent_from_the_snapshot_wire` |
| TP-AGPANEL-17 | The resume recipe is frozen at close time; the terminal's own reported session outranks the session the tab was born resuming | The report tracks compacts and forks, the birth id does not; and at revival time both are gone (#46's class) | `the_harvest_freezes_the_terminals_own_session_recipe` |
| TP-AGPANEL-18 | A revival that cannot answer for its directory (none recorded, or gone from disk) is refused with a reason and the row stays dormant | Guessing lands in `$HOME` — the exact #46 failure; and a refusal that flips state bricks the row | `a_revival_without_a_livable_directory_is_refused_and_stays_clickable` |
| TP-AGPANEL-19 | A ghost without a session recipe is refused with a reason instead of launching a stranger | A fresh agent wearing the old name is not a revival; the user loses the conversation and trusts the panel less | `a_revival_without_a_session_recipe_is_refused` |
| TP-AGPANEL-20 | A ghost whose conversation already lives in a wired tab focuses that tab and leaves the graveyard — no twin | #45's lesson wearing grey: one session, one surface. A twin tab splits the conversation's identity | `a_ghost_whose_session_is_already_open_focuses_it_instead_of_twinning` |
| TP-AGPANEL-21 | A revival launches along its own agent's existing road — claude with the chat drawer's exact launch, others with the restore road's plan | A third argv invented for revival drifts from the other two the next time either changes | `a_revival_launch_matches_the_agents_existing_road` |
| TP-AGPANEL-22 | The graveyard renders after the living rows: a separator, then plain-text ghost rows that dim AND lean (italic); a reviving row wears a static ellipsis | Dim alone collides with the passive agent token, which already dims — the third class stops reading as a class; an animated ghost would need a ticking backend (R6's line) | `closed_agents_render_dimmed_under_a_separator` |
| TP-AGPANEL-23 | The ghost click resolves through the painter's own layout function (`closed_agent_row_slots`); the separator and off-rows name nobody | A hit box beside its paint: clicks revive the wrong ghost or dead furniture becomes a button | `a_click_on_a_ghost_row_names_that_ghost` |
| TP-AGPANEL-24 | Revival is a protocol method (`closed_agent.revive`); refusals answer with a stable code and readable reason | A private TUI gesture leaves the PWA a second-class client, and a refusal nobody can read is a silent bug | `revival_travels_the_api_road_with_visible_refusals` |
| TP-AGPANEL-25 | The tab-close road births ghosts for its agent panes before the cascade removes them; a refused close (the last tab) births nothing | An agent closed by its tab vanishes without a ghost — or worse, a refused close leaves a living agent haunting the list | `closing_a_tab_leaves_its_agent_in_the_ledger` |
| TP-AGPANEL-26 | The grey row wears the tab's own name when it has one; the agent kind is only the fallback | Three claude ghosts in one list are indistinguishable — the row answers "an agent closed", never "which one" | `a_ghost_wears_the_tabs_own_name_when_it_has_one` |
| TP-AGPANEL-27 | The workspace card's "+" menu answers on the production road: an agent starts a chat in that workspace's own checkout, and the worktree verbs queue their requests. | The arm lived only in the `#[cfg(test)]` sibling of `apply_context_menu_action`, so every test passed while the menu opened, offered its agents, and started nothing — the affordance was dead in the product and no test could see it. This is the second instance of that shape in this tree (TP-DAILY-11 was the first), which is why the rule is now to grep BOTH bodies for every `ContextMenuKind` rather than to trust the one the tests reach. | `the_workspace_plus_menu_starts_a_chat_on_the_production_road` |
| TP-AGPANEL-28 | An agents-panel row offers "Move to..." **only when** the ledger knows which chat it is running (`Tab::resumed_session_id`), the identity is read when the menu OPENS, and the verb opens the same target picker the chat row opens — on both the test and the production body. | This is the surface where running agents actually are, and "send this one somewhere" is the request that started the feature. But the identity exists only for a tab opened to resume a known session; a freshly started chat has none yet, and offering a move that cannot be honoured is worse than offering nothing. Reading the identity when the verb fires instead of when the menu opens repeats TP-AGPANEL-06's failure: a menu outlives the panel it was opened from and a late lookup answers for whatever moved into that slot. Adding the item without adding the production arm ships a dead affordance — #91 was exactly that, in exactly that body. | `an_agent_row_with_a_known_chat_offers_to_move_it`, `the_agent_row_menu_offers_exactly_the_close_verb`, `moving_from_an_agent_row_opens_the_picker_on_the_production_road` |
| TP-AGPANEL-29 | When a graveyard exists, the panel's scroll ceiling reserves the last two rows for it (separator + one ghost); with no ghosts the ceiling is unchanged. | The ceiling used to be computed from the living rows alone and the graveyard was painted only into whatever space they left over. On a panel that is always full — fifty-nine panes were measured on the machine this was reported from — those two rules meet badly: leftover space never appears, and NO scroll position reaches the ghosts. The section was unreachable, which is indistinguishable from absent, and that is how it was reported. Reserving unconditionally would be the opposite error: an empty graveyard would open a gap under the panel that promises more below and delivers nothing. One row is never enough either — a separator with nothing under it is the rendering bug the graveyard contract already forbids. | `a_full_panel_can_still_be_scrolled_down_to_its_graveyard`, `with_no_ghosts_the_panel_ceiling_is_unchanged`, `a_short_panel_still_shows_its_graveyard_without_scrolling` |
| TP-AGPANEL-30 | The graveyard is written to `closed-agents.json` and read back at startup: what was saved is what returns. | The ledger lived entirely in memory, so every restart emptied it — and on this machine a restart is not rare, because each delivery replaces the running server through `live-handoff`. Measured 2026-08-16 03:10: two ghosts stood in the panel before a delivery and zero after it, including the agent the user had closed minutes earlier. Without a store, "let me see the ones that opened and closed in the last month" is unreachable at any capacity. | `a_saved_graveyard_reads_back_unchanged` |
| TP-AGPANEL-31 | A ghost older than the retention window (30 days) is dropped, measured from now rather than from the write. | Age is the bound the request was about. Measuring from the write instead would let a store that sat untouched for a year come back whole on the next start, and a ghost that old points at a directory that has usually moved on — a row that can only refuse. | `a_ghost_older_than_the_window_is_dropped` |
| TP-AGPANEL-32 | Pruning sorts newest-first and the count bound (500) evicts the oldest. | The panel draws in this order, so an unsorted store would paint the graveyard in write order and the "oldest first" eviction would drop whatever happened to be appended last instead of the oldest death. Persistence is also not permission to grow without limit: a machine that closes agents all day would turn a convenience into a disk-space bug, the same reasoning `MAX_CHATS_PER_WORKSPACE` carries next door. | `pruning_keeps_the_newest_first_and_evicts_the_oldest` |
| TP-AGPANEL-33 | An unreadable or foreign-versioned store starts empty instead of failing. | This file is read on a path that runs at every startup. A graveyard that refuses to load would take the whole panel down with it, and an unknown layout guessed at is worse than one declared unreadable. An empty graveyard is a normal state; a failing start is not. | `an_unreadable_store_starts_empty_instead_of_failing`, `a_store_from_another_version_starts_empty`, `a_missing_store_is_an_empty_one` |
| TP-AGPANEL-34 | `RevivalState` never crosses the disk — every loaded ghost starts `Dormant`. | Revival state describes THIS process. A spawn that was in flight when the server was replaced did not survive the replacement, and a row that came back claiming `Reviving` would be inert forever: it refuses every click with `RevivalInFlight`, waiting on a claim held by a process that no longer exists. | `a_loaded_ghost_starts_dormant_whatever_it_was_doing` |
| TP-AGPANEL-35 | The store is written by the trigger (an agent closed), not by a timer — and `--no-session` suppresses the write. | The module is explicit that nothing periodic belongs to it. A save that waited for the next session snapshot would lose the newest death to exactly the event that makes losing it likely: the server being replaced. The `--no-session` half is what keeps the write off a real config directory in every fixture that closes a pane — the shape of defect TP-DRAW-13 was, one directory over. | `a_sessionless_run_does_not_write_the_graveyard` |
| TP-AGPANEL-36 | The store is read at startup on the same `--no-session` terms. | Without it, hundreds of fixtures built on `App::new(.., true, ..)` would inherit whatever the machine's real graveyard happened to hold, and a measurement would move whenever the person running it closed an agent. | `a_sessionless_start_does_not_read_the_graveyard` |
| TP-AGPANEL-37 | A round trip keeps everything a revival needs: cwd, workspace key, and the session ref including its kind. | The cwd is the whole difference between reopening where the user worked and reopening in `$HOME` (#46). The session ref is what reattaches the conversation instead of presenting a stranger, and an id resumes differently from a transcript path — a ghost that survives a restart without them is a row that can only refuse. | `a_round_trip_keeps_what_a_revival_needs` |
| TP-AGPANEL-38 | Loaded history is not discarded by the next close: the ledger's capacity is the store's bound, not the panel's row count. | The ring held eight, sized for the sidebar's height — a reason belonging to the drawing layer, which enforces its own bound anyway (`closed_agent_row_slots` hands out only the rows the body has). Once a store existed, eight became silent data loss: a run loads a month of history from disk and the very next agent that closes truncates it back to eight. Found by mutation while the store was being written; no existing test covered it, because before the store there was no history to lose. | `a_loaded_history_survives_the_next_close` |
| TP-AGPANEL-39 | The graveyard is seeded from the transcript rows already loaded at startup: conversations with no headstone get one, carrying the directory and session ref so they revive like any other; a conversation that still has a tab is skipped, and one already remembered is not derived twice. | Persistence only remembers deaths it witnessed, and it started witnessing them the day the store was added — so "let me see the ones that opened and closed in the last month" would have stayed empty for a month. The surviving record of everything before that is the transcript store, which `App::new` has just parsed for the chat drawer; deriving from those rows rather than re-walking the directory keeps a startup path from reading the same files twice. The live filter is not an optimisation: without it the same chat stands in the panel twice, once as a running agent and once as its own headstone. And a witnessed death outranks a derived one, for the reason `record_closed` already dedups — two ghosts with one identity leave the user guessing which revives, and the answer would be neither reliably. | `a_conversation_with_no_headstone_gets_one_from_the_store`, `a_live_conversation_is_not_given_a_headstone`, `a_conversation_already_remembered_is_not_derived_twice` |
| TP-AGPANEL-40 | The graveyard is loaded for a handed-off server too: `new_from_handoff` seeds it after the restore, not behind the `no_session` flag it passes. | This codebase has more than one constructor and the live server normally comes from the other one. `new_from_handoff` calls `new` with `no_session = true` — correctly, since it restores its session from a snapshot rather than from disk — and the graveyard fell behind that flag with it. But the store is not the session: it outlives every handoff, and on this machine a handoff happens on every delivery, so the panel would have been empty on the only path that matters. Measured 2026-08-16 05:29 against the live server: six chats in the ledger, none of them live, zero ghosts in the panel. Seeding after the restore rather than before it matters separately — the live filter reads the tabs the handoff just brought back, and running first would give every restored chat a headstone of its own. | `a_handed_off_server_loads_the_graveyard` |
| TP-AGPANEL-41 | One identity leaves one headstone, and the surviving row is the most recent sighting. | Seeding walks the drawer's rows per directory key, and one conversation can appear under two of them — `$HOME` and a checkout inside it both list it — so the same chat was derived twice and stood in the live panel twice (measured 2026-08-16 06:20: 63 ghosts, with an adjacent pair carrying identical titles and timestamps). Deduped in `prune` rather than at the seeding site because that is the single gate every record passes, whether it was witnessed, derived, or read back from disk; a guard at the seeding site would leave the store's own duplicates untouched. Newest wins because the list is already sorted by then, so the first sighting of an id is its most recent one. | `one_identity_leaves_one_headstone` |
| TP-AGPANEL-42 | The living rows and the headstones are laid out by ONE function, which takes the fields it needs (`AgentTokenContext`) rather than an `AgentPanelEntry`. A headstone fills the identity slot with its label and the state slot with its age; slots it cannot answer stay empty, exactly as they do for a living pane that has no tab or pane label. | The user asked for the closed agents to be drawn "in the same format as the open ones, the only difference being that they are a faded grey", and the reason they were not is that a second drawing path existed and had drifted all the way to a bare one-line `Paragraph` while the living grew token rows, truncation and gaps. Two paths cannot be kept in step by intention; one function cannot drift from itself. Taking the fields rather than the struct is what makes that possible without lying: a closed agent has no pane, tab or workspace index, and inventing three so it could be an `AgentPanelEntry` would leave a fabricated `PaneId` one careless read away from resolving to a live pane. | `a_headstone_is_as_tall_as_the_row_layout_says`, `closed_agents_render_dimmed_under_a_separator`, `missing_custom_tokens_elide_rows_and_separators` |
| TP-AGPANEL-43 | The panel scrolls ONE sequence: the living rows, then — only when there is at least one headstone — a divider and the headstones. Scroll ceiling, visible count, painting and both hit tests all read that sequence, and a press anywhere on a headstone's card resolves it, not only on its first row. | Measured on the reported machine: 62 headstones on disk, of which a handful could ever be seen. The graveyard was not part of the scrolled list — the ceiling reserved two rows for it and the painter filled whatever the living left over, newest first, clipping the rest. On a panel the living always fill there is no leftover space, so NO scroll position reached the others: they were unreachable rather than merely below the fold, and "I want to see them one by one, in a scrollable area" was not satisfiable whatever the rows looked like. Three separate mechanisms (a reserved-rows constant, a leftover-space walk, a second paint loop) became one list, so a headstone can no longer be reachable by one and invisible to another. TP-AGPANEL-29's rule survives structurally rather than as a constant: the divider is emitted only alongside a headstone, so a divider dividing nothing is impossible to construct. | `every_headstone_is_reachable_at_some_scroll_position`, `a_press_on_the_lower_half_of_a_headstone_still_hits_it`, `a_full_panel_can_still_be_scrolled_down_to_its_graveyard`, `a_short_panel_still_shows_its_graveyard_without_scrolling`, `with_no_ghosts_the_panel_ceiling_is_unchanged`, `a_click_on_a_ghost_row_names_that_ghost` |
| TP-AGPANEL-44 | A headstone's state slot carries how long ago it closed, in the panel's own relative vocabulary; the static ellipsis a revival wears rides the NAME, not the age. | Where a living row says what it is doing, a closed one can only say when it stopped — the slot cannot stay empty without collapsing the layout and cannot carry a live state it does not have. The age is also the half of the request this section exists to answer ("let me see the ones opened and closed in the last month"). The ellipsis stays on the name because the name is the one thing a headstone always has: put it on the age and a row layout that asks for no state text would drop the only sign that a revival is under way, which is precisely what happened the first time this was written. | `closed_agents_render_dimmed_under_a_separator`, `a_headstone_never_wears_the_active_accent` |

Source markers: `src/ui/sidebar.rs::render_agent_detail` (style block) ·
`src/app/input/mouse.rs` (right-click road) · `src/app/input/modal.rs` (both
dispatch roads) · `src/app/state.rs::ContextMenuKind::AgentEntry` ·
`src/app/closed_agents.rs` (ledger + revival state machine) ·
`src/app/actions.rs::note_agent_closed` (harvest) ·
`src/app/projects.rs::open_project_chat_tab_with_argv` (the shared spawn road
revival rides).
| TP-AGPANEL-45 | A graveyard row answers a right-click with its own menu, carrying the ghost's agent id and offering exactly two verbs in order: Revive, then Forget. Revive parks the same request the left-click road raises; Forget drops the record from the ledger and marks the session dirty, reporting nothing when the row has already gone. Both context-menu bodies route through one shared body. The resolver is matched before the living rows, and a press on a living row still gets the living menu. | The graveyard only ever grew. A record arrives when an agent closes and leaves only by ageing past capacity or by being revived — and reviving is the opposite of what someone wants when they are looking at a row they are finished with. Measured on the reporting machine: 62 records standing, none removable, which is why the panel that TP-AGPANEL-43 made scrollable was scrollable through a list nobody could shorten. Carrying the id rather than an index is the rule TP-AGPANEL-06/28 already state: the panel refreshes under an open menu and a late lookup answers for whatever moved into that slot. Forget must tolerate a missing row for the same reason — the menu can outlive the list it was opened from — and reporting success for a removal that did not happen would mark the session dirty for nothing. "Move to..." is deliberately absent: a ghost has no running tab to move, and filing its conversation somewhere is the chat row's verb. Ordering Forget last is TP-MOD-08/26's rule, the one item that takes something away goes at the bottom where a mis-aimed press cannot reach it first. | `a_graveyard_row_offers_revive_then_forget`, `both_context_menu_bodies_answer_the_graveyard_verbs`, `forgetting_from_the_menu_removes_the_record`, `forgetting_one_ghost_leaves_the_others`, `forgetting_an_unknown_ghost_reports_nothing` |
| TP-AGPANEL-46 | A press on a headstone resolves through the SAME filtered sequence the painter draws: `visible_closed_agents()`, not the raw ledger. A record whose chat is hidden takes no row and takes no hit. | The panel's rows are built over the visible ghosts (hidden chats — e.g. routine chores — are filtered out), but the click resolver walked the unfiltered ledger with the visible index. With one hidden record at the front, every ghost below it resolved to its neighbour: the user clicked one conversation and a different one was revived, in that other record's directory. Reported live 2026-08-20 ("clicking the off-mode agents routes me to different agent chats") — and the user's own hypothesis, previously hidden routine agents, was exactly the trigger. `visible_closed_agents` already documents the rule: one gate for every pass that is addressed by index into the others; the resolver was the forgotten fourth pass. | `a_hidden_record_does_not_shift_the_ghost_a_click_names`, `a_hidden_record_at_the_tail_changes_nothing` |
| TP-AGPANEL-47 | The agent row's context menu opens with "Rename tab..." — before the move verb, with the irreversible close still last — and the verb rides the exact road the tab strip's rename takes: focus the row's workspace and tab, open the same name input. Offered with or without a chat behind the tab, on both dispatcher bodies (keyboard and mouse). | The user asked to rename a running agent's tab from where the agent is listed; the only road went through the tab strip. One rename semantics, not two — a second input would let the two roads drift — and #91's lesson stands: an arm missing from the mouse body is a dead affordance that looks shipped. | `the_agent_row_menu_offers_exactly_the_close_verb`, `the_agent_row_rename_verb_opens_the_tab_name_input`, `the_agent_row_rename_verb_works_on_the_mouse_road_too` |
