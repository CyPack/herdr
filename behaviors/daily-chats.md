# Daily chats — the directory no checkout claims

A chat started outside every repository files itself under `$HOME`. Since
TP-WSID-01 made `effective_cwd` prefer the checkout over the birthplace, no
workspace holds that directory any more — so nothing on the sidebar's side
ever asks to read it. Measured 2026-08-12 on the live machine: **1266
transcripts** under `$HOME`, **0** workspaces holding it, and the chat ledger
carrying exactly one key (a checkout's). Those conversations were reachable
from nowhere.

This family owns the half of that fix whose loss would be silent: the read
itself. A merge that quietly re-tied the read to the workspace list would not
fail a test unless one names this — the drawer would simply go back to showing
nothing, and it would look like there had never been anything there.

| ID | Behavior | Breaks if lost | Verified by |
|---|---|---|---|
| TP-DAILY-01 | The daily directory (`AppState::daily_chat_cwd`, `$HOME` in production) is read on every chat-row merge **whether or not a workspace lives there**, under the same glance/open budget as any other drawer (12 rows closed, 60 opened all the way), unioned with the ledger one-row-per-chat, and a directory that is missing — or a client with no home at all — costs nothing | The read follows the workspace list again and the section is born dead: `$HOME` has no workspace and never will, so "read what the workspaces ask for" finds none of these chats. The failure is invisible — an empty section reads as "no chats yet", not as a bug | `daily_chats_are_read_even_when_no_workspace_lives_in_that_directory`, `a_daily_directory_that_is_not_there_costs_nothing`, `an_open_daily_drawer_reads_past_the_glance_limit`, `a_daily_chat_the_ledger_also_saw_stays_one_row` |

| TP-DAILY-02 | The section is emitted above the whole tree — header first, then its chats — and an empty section is not drawn at all | These chats belong to no checkout, so a row that is not first is a row that is nowhere; and a header promising content that is not there reads as a broken surface on every machine that never started a chat outside a checkout | `the_daily_section_is_emitted_above_the_tree`, `a_daily_section_with_no_chats_is_not_drawn_at_all`, `the_daily_section_draws_a_header_its_count_and_its_chats` |
| TP-DAILY-03 | The fold is per-display and takes every row below the header with it; the section's rows keep area vectors of their own, carrying no `ws_idx` | Folding on a laptop would close the monitor's section — the complaint per-display surfaces exist for; and a daily row folded into the workspace-indexed vectors makes every press resolve as some other checkout's chat | `folding_the_daily_section_takes_every_row_below_it`, `daily_rows_stay_out_of_the_workspace_indexed_vectors`, `the_daily_switches_toggle_both_ways_and_carry_the_read_budget` |
| TP-DAILY-04 | Five chats and a switch; opening the section lists them all and moves the read budget with it, and the switch is the way back | Without the bound a machine with a thousand home transcripts buries the tree under its own history; without the budget the switch promises older chats the parse never fetched; without the way back it is not a switch | `the_daily_section_lists_five_chats_and_offers_the_rest`, `the_daily_switches_toggle_both_ways_and_carry_the_read_budget` |
| TP-DAILY-09 | A workspace sitting in the daily directory does **not** silence the section — the read answers for the directory, never for who else is standing in it. | This family once carried the opposite contract, and reversing it is the whole point: measured 2026-08-15 on the live machine, ten workspaces had been born in `$HOME` and seven of them held no checkout, so `effective_cwd` returned `$HOME` and the claim test fired on every render — the section had never once been drawn for its own user. Restore the claim test and the feature goes dark again on exactly the machines that need it, silently, because an undrawn section is indistinguishable from an empty one. The duplication the old rule feared was already there: each of those workspaces reads this same ledger key, so the list was on screen seven times while its one canonical home was not | `a_workspace_sitting_in_the_daily_directory_keeps_the_section` |
| TP-DAILY-06 | Focus hides the section — unless one of its chats is running, which keeps it visible | Daily chats sit in no tree, so they go quiet with everything else the filter narrows away; but a filter may narrow what you see and never hide where you are | `focus_hides_the_daily_section_unless_one_of_its_chats_is_running` |
| TP-DAILY-07 | A daily row resumes in the daily directory, switches to the chat's live tab instead of resuming it twice, and answers a stale index with nothing | Substituting the active workspace's path is #46 with the roles reversed; a second tab on one conversation is #45 in another surface; and the list can refresh between the frame a person clicked and the click arriving | `a_daily_chat_resumes_in_the_daily_directory`, `a_daily_chat_already_running_is_switched_to_rather_than_resumed`, `a_click_on_a_daily_row_that_no_longer_exists_does_nothing` |

| TP-DAILY-10 | The header's trailing column is a "+" that opens the agent menu, matched **before** the fold, drawn only while the mouse owns the panel (the count steps two columns left for exactly that long), and reserved not at all below six columns | Both gestures live on one row, so order is the behavior: match the fold first and every press aimed at the plus folds instead, leaving a section that can show old chats and cannot start a new one — the half of the surface a person actually reaches for. Draw the plus without capture and an invisible cell swallows presses meant for the fold; draw it in a five-column header and it looks pressable while doing nothing | `a_left_press_on_the_daily_plus_opens_the_menu_instead_of_folding`, `a_header_too_narrow_reserves_no_daily_plus` |
| TP-DAILY-11 | The "+" menu offers the chat agents and nothing else, opens on the persisted default, and the chosen agent starts a chat rooted at the daily directory with no session to resume; a client with no home directory asks for nothing | The daily directory is not a checkout, so a worktree verb would be an offer the tree cannot keep. Rooting the new chat at the active workspace is #46 with the roles reversed — the conversation would be born in a checkout the person was not looking at — and a session id here would reopen an old chat instead of starting the new one that was asked for | `a_new_daily_chat_starts_in_the_daily_directory`, `the_daily_plus_offers_agents_only_and_starts_on_the_default` |
| TP-DAILY-12 | The area's header is drawn in the containers' dialect — accent-coloured disclosure arrow as its own span, its own icon (`spaces.icons.daily`, a home by default), bold name truncated short of the trailing chrome — and it wears the `⋯` every container header wears, whose menu ("New chat...", fold verb) is the same one a right-click on the row opens. | Drawn as a plain heading it read as a caption ABOVE the tree rather than as the first place IN it, which is exactly what the user reported after the section shipped ("branch gibi gözüküyor, en üstte falan değil"). Without the `⋯` it was the only header on this sidebar a person could not act on; with the chat glyph it read as one more conversation rather than as where their day lives. If the dots and the right-click ever open different menus the two doors have drifted, which is the failure TP-DOTS-04 exists to prevent | `the_daily_header_is_drawn_in_the_container_dialect`, `the_daily_header_answers_the_dots_and_the_right_click_with_one_menu`, `the_daily_header_menu_folds_and_starts_chats` |
| TP-DAILY-08 | The phone drawer draws the same section in the same order, with a plain section title rather than a fold; the cursor stops on its chats and on nothing else there, and a tap resumes in the daily directory | Two surfaces walking one tree is the point — a phone-only omission means a chat reachable from one screen and not the other; and a cursor resting on a title that does nothing is a press that goes nowhere, the rule `SectionTitle` and `ChatNote` already keep | `the_phone_drawer_carries_the_daily_section_at_the_top`, `the_phone_cursor_stops_on_daily_chats_but_not_on_their_title`, `tapping_a_daily_chat_resumes_it_in_the_daily_directory` |
| TP-DAILY-13 | A workspace whose **effective** directory is the daily directory is emitted **under the daily area, indented, and once** — skipped by the tree walk, re-emitted by the section, folded with it, still clickable, and given no chat drawer of its own | Measured on the live machine 2026-08-15: seven of thirty workspaces stood in `$HOME`, none of them holding a checkout. The tree drew all seven as branch rows beside real repositories, every one listing the same twelve chats, and all seven opened and closed together because the drawer's fold state is keyed by the ledger key they share (TP-WSID-03) — the duplication the user reported twice. Lose the tree skip and the row is drawn in both places, which is worse than the original complaint. Lose the section emit and the row is drawn nowhere: those seven workspaces held nine tabs and fifteen panes, one a blocked agent still waiting, and the list is the only way in. Compute ownership from `worktree_space` alone and it finds nothing at all — every one of the seven answers with its birthplace, which is how the first measurement of #88 came back "zero workspaces claim `$HOME`" while seven did. **Filter the workspace set at the source instead of skipping the walk and every surface reading that set loses the row** — this shipped once, as the first TP-DAILY-13, and was reverted when it took API-created workspaces off the client frame; `moving_the_row_never_removes_it_from_the_shared_set` is the guard that makes the same mistake impossible to land twice. Give the moved row a drawer and #96 returns intact, because that drawer reads the very ledger the rows above it came from | `a_workspace_standing_in_the_daily_directory_belongs_to_the_area`, `a_checkout_that_points_at_the_daily_directory_also_belongs_to_the_area`, `without_a_daily_directory_no_workspace_belongs_to_the_area`, `a_workspace_that_lives_elsewhere_stays_out_of_the_area`, `an_undrawn_daily_area_owns_nothing`, `the_daily_area_s_own_workspace_sits_under_it_and_not_in_the_tree`, `moving_the_row_does_not_touch_the_workspace_itself`, `folding_the_daily_area_folds_the_workspace_it_owns`, `moving_the_row_never_removes_it_from_the_shared_set`, `without_a_daily_directory_the_tree_keeps_its_rows`, `the_row_under_the_daily_area_is_still_clickable` |
| TP-DAILY-15 | A daily-area row with no name of its own takes one from what is inside it: a named tab outright when it is the only tab, the first named tab plus a count of the rest when there are several, otherwise the agent running in one of its panes. Only `custom_name` counts as a tab name, and a blank one does not count at all. | The daily area collects every workspace standing in one directory, so the directory's name is the one thing they all share and deriving from it names them all alike. `main` seven times was #99; `user` seven times is the same sentence in a different word, which is exactly what the user answered with ("7 tane ayni branch olusu normal bir sey mi?"). Reach for `tab_display_name` instead of `custom_name` and it is worse than useless: that accessor falls back to the tab's ordinal, so six workspaces would all be named `1` — measured on the live machine, six of the seven held exactly one unnamed tab. A blank custom name must be rejected for the same reason a missing one is: whitespace would outrank a perfectly good agent label and draw an empty row. | `a_lone_named_tab_names_the_row_outright`, `an_unnamed_tab_yields_to_the_agent_running_in_it`, `a_multi_tab_row_names_the_first_and_counts_the_rest`, `a_blank_tab_name_does_not_count_as_named`, `a_row_with_no_content_derives_no_name`, `a_daily_row_wears_the_name_of_the_tab_inside_it`, `a_named_daily_workspace_keeps_the_name_its_owner_gave_it` |
| TP-DAILY-16 | Rows that still resolve to one name are numbered from the second onwards, and the ordinal steps over any name already in the set. A workspace the user named keeps that name but still takes part in the numbering. | A name is only useless *because another row has it too*, which makes this a question about the set and not about any row — resolve it per row and the answer cannot be known. Measured on the live machine: five of the seven workspaces ran nothing but `reviewr`, so content alone still lands all five on one name and the reported defect survives the fix that was supposed to remove it. Appending blindly is the same bug from the other side: a row literally named `reviewr 2` would be shadowed by an ordinal minted for a different row, producing the duplicate this rule exists to prevent. Numbering a name that appears once announces a series of one. | `repeated_labels_are_numbered_so_every_row_is_addressable`, `a_label_that_appears_once_is_left_alone`, `numbering_steps_over_a_name_that_already_reads_like_an_ordinal`, `seven_rows_in_one_directory_do_not_all_read_alike` |
| TP-DAILY-17 | The TUI's "new workspace" intent — BOTH roads, the mouse/key affordance and the `request_new_workspace` loop, through one function — opens a new TAB in the workspace already standing in the daily directory instead of creating a second one, but only when that workspace is UNNAMED and only when the new workspace would have landed in that directory anyway. `workspace.create` itself is unchanged. The membership predicate lives on the state, with the sidebar wrapping it in its own visibility test. | Root cause, measured: `new_cwd = "follow"` makes a new workspace inherit the focused pane's directory, so a `$HOME` workspace produces the next `$HOME` workspace, which produces the next. The reviewr plugin's own `pane-<wsId>` files date them — 07:29, 07:39, 07:48, 07:55, 08:01 on 2026-08-14, five in 32 minutes, each spawned from inside the last, and 26 such workspaces since 07-27. Six of the seven held nothing but a shell and a plugin pane. Putting the rule in `workspace.create` would break the API contract silently: a script that asks for a workspace must not receive a tab, and the response type says `workspace_created`. Exempting named workspaces is not politeness — a name is a stated identity, and adopting past it would overrule a decision the machine did not make. Guarding on the target directory is load-bearing in the other direction: a "new workspace" pressed inside a repository must still make a workspace there, which would be a far larger defect than the rows being removed. Two TUI roads is exactly the shape of #91, where a verb lived in one body and did nothing in the product. | `a_second_workspace_in_the_daily_directory_is_adopted_instead`, `a_named_workspace_in_the_daily_directory_is_not_adopted`, `the_first_workspace_in_the_daily_directory_is_still_created`, `a_new_workspace_outside_the_daily_directory_is_untouched` |
| TP-DAILY-18 | The daily area draws ONE of the workspaces standing in its directory and folds the rest behind a `… N more here` switch with its own row, its own hit target and its own state; the phone draws the same note. The visible one is the workspace currently active, when the active one is among them. One workspace draws no switch. | The daily directory is a single place — its chats are keyed by that directory, which is why these rows have no drawer of their own (#96) — so several rows for it say one thing several times. Seven of them was the report, and naming them apart (TP-DAILY-15/16) made the repetition legible without making it less repetitive: "hepsinin içinde aynı chatler var, fark ne ki? bu kadar spam gibi 7 tane". Folding rather than dropping is the whole difference between a fix and the #88 failure: those six held fifteen panes and one blocked agent, and a row nobody can reach is a row that is not there. The active one leads because a tidy-up that hides the row you are working in has made the surface worse. The switch needs its own rect: sharing the chat switch's would make a press on either fire both, the confusion TP-TREE-01 split rows to prevent. A switch drawn for a single workspace is a control that lies. | `the_daily_area_shows_one_workspace_and_a_switch_for_the_rest`, `expanding_the_switch_reveals_every_workspace_standing_there`, `a_single_daily_workspace_offers_no_switch`, `the_workspace_you_are_in_is_the_one_left_visible` |
| TP-DAILY-14 | A daily-area row wears its own name; a checkout under a repository header still reads as its branch. | `indented` says a row is drawn one level in, and it was carrying a second meaning it never earned — "this row is a checkout under a repository header", which is what makes a branch name the right label there. The daily area's rows share the flag and nothing else: they have no checkout, so the branch they showed came from whatever repository happened to contain the daily directory. On the machine this was reported from that was `$HOME`, and seven rows all read `main` — the user asked what branches had to do with anything. #94 caused it without meaning to: moving those rows under the daily header moved their labels too, from `user` to `main`, and the before/after table recorded the change as if it were the indent. The group-child case is asserted alongside because that is what tells sibling checkouts apart and the fix must not reach it. | `a_daily_area_row_keeps_its_own_name_instead_of_a_branch`, `a_group_child_row_still_reads_as_its_branch` |
| TP-DAILY-19 | The daily area's header menu offers **Merge workspaces here** whenever two or more UNNAMED workspaces stand in its directory, and never otherwise. Running it moves every pane of the others into new tabs of ONE of them — the active one when the active one is among them, otherwise the first — via `pane.move`, so no workspace is closed by this verb, no terminal is killed, and a pane's old public id still resolves afterwards. Named workspaces and workspaces outside the daily directory are never touched. Both context-menu bodies set one request flag and the App loop does the work. | TP-DAILY-17 stopped new ones being born and TP-DAILY-18 stopped them filling the sidebar, but neither removed the ones already there — the user asked how to get rid of them ("nasil kaldiricaz"), and folding a row away is hiding it, not removing it. The mechanics matter as much as the verb: the earlier plan said to move TABS via `tab.move`, which is workspace-INTERNAL reordering — a tab's public id contains its workspace's id, so moving a tab across workspaces is an identity change, the refactor-risk class CLAUDE.md names by hand. `pane.move` already does the whole job and was written for it, down to aliasing the source pane's public id so a plugin holding it keeps resolving. Collect the public ids BEFORE the first move or the second move addresses the wrong workspace: an emptied workspace is removed from the list and every index after it shifts. Doing the work inside the menu bodies instead of behind a flag would give the verb two implementations, because the `#[cfg(test)]` body is handed an `AppState` and cannot dispatch at all — that is #91 exactly. Skipping named workspaces is the same rule adoption keeps (TP-DAILY-17): a name is a stated identity. Moving with `focus: true` would throw the screen around once per pane, fifteen times on the reported machine. | `every_unnamed_workspace_in_the_daily_directory_is_mergeable`, `a_single_daily_workspace_is_not_a_merge`, `a_named_workspace_is_never_merged_away`, `the_merge_target_is_the_active_workspace_when_it_is_one_of_them`, `the_merge_target_falls_back_to_the_first_when_the_active_is_elsewhere`, `workspaces_outside_the_daily_directory_are_never_mergeable`, `the_daily_header_offers_the_merge_only_when_there_is_something_to_fold`, `both_context_menu_bodies_request_the_daily_merge`, `merging_leaves_one_workspace_and_carries_every_pane_into_it`, `the_workspace_you_are_in_is_the_one_that_survives`, `merging_leaves_a_named_workspace_alone`, `merging_a_single_workspace_changes_nothing`, `a_merged_pane_still_answers_to_the_public_id_it_had`, `a_workspace_in_a_repository_is_untouched_by_the_merge` |
| TP-DAILY-20 | Chat labelling is configured by four `[ui]` keys. `routine_chat_markers` lists the structural openings a chat writes about itself and is matched against the **start** of the first message; `routine_chat_prompt_patterns` adds further openings, prefix-matched after case and spacing are normalised; `routine_chat_repeat_threshold` says how many chats must share one opening before the repetition alone counts, with `0` turning that layer off; and `hidden_chat_labels` says which labels the daily section leaves out, with an empty list bringing every chat back. The markers ship holding the two declarations measured here, and `<local-command-caveat>` is deliberately **not** one of them. | The daily section shows five chats out of a directory holding 1293, ordered by what was touched last, so a task that runs hourly outranks real work opened a month ago every single time — the rows are not lost, they are permanently second. Measured against that directory: 348 chats declare themselves structurally and another 185 repeat one normalised opening, about 42% of everything there. Lose the defaults and a fresh install classifies nothing, which makes the whole feature something a person has to discover before it does anything. Lose the reading of a key and it is worse than absent: the reader edits the file, the sidebar does not change, and nothing says why. Ship `<local-command-caveat>` in the markers and the single most common opening measured here (173 chats) disappears by default — but the thing that writes it is the person's own `!` command, so that would be hiding their own work and calling it a chore. Naming one key must leave its three neighbours at their defaults, or writing one line silently unwrites the rest of the surface. | `routine_chat_keys_parse_and_default` |
| TP-DAILY-21 | A chat is classified from its **opening** — the normalised shape of its first message — by prefix and by prefix only. Markers and hand-written patterns match at the START of the opening, both put through the same normalisation the opening itself went through; a rule that normalises to nothing matches nothing at all; and an opening no rule recognises stays **unlabelled**, which is a fourth state and not a synonym for `context`. | Matching anywhere instead of at the start collapses the difference between a chat that declares itself and a person writing *about* automations — this very feature was designed in a conversation that says `<scheduled-task` in the middle of a sentence, and an anywhere-match would have hidden it. Comparing rules unnormalised is the quiet half of the same failure: a pattern typed `Claude Code Update` is correct, reads correct, and matches nothing, because the opening it is compared against was lowercased on the way in. The blank-rule guard is the one that fails loudest — an empty string is a prefix of every string, so one stray blank line in either list marks every chat routine and empties the daily section entirely, arriving at the exact outcome the feature exists to prevent by way of a typo. And claiming `context` for unrecognised openings would invent a fact: a rule not recognising a chat says nothing about what that chat is, and an unlabelled chat is drawn. | `an_opening_that_announces_a_scheduled_task_is_routine`, `a_marker_mentioned_mid_sentence_is_not_a_declaration`, `without_markers_the_structural_rule_matches_nothing`, `an_opening_that_starts_with_a_known_pattern_is_routine`, `a_pattern_matches_through_case_and_spacing`, `a_blank_rule_marks_nothing_rather_than_everything`, `an_unrecognised_opening_is_left_unlabelled`, `label_names_are_read_and_unknown_names_are_nothing`, `unreadable_hidden_labels_are_dropped_rather_than_kept` |
| TP-DAILY-22 | Openings are remembered by **session id**, not per row, and the whole classification is recomputed from scratch at the end of every chat sync and on every config reload. The repetition rule counts chats sharing one normalised opening across **every** directory at once, and `0` switches that rule off without switching the declared ones off with it. | Keying by row is the bug that makes the repetition rule lie: a chat listed under two directories would count twice, and with a threshold of three a single conversation opened in two places is most of the way to declaring itself a chore. Counting per directory is the same failure from the other end — the automations this exists for are started by timers that do not care which checkout anyone is standing in, so three copies of one chore in three places would each be counted alone and none of them called repeated; measured here, the largest such opening appears 73 times spread across directories. Recomputing rather than patching is what makes `reload-config` mean anything: the verdicts already stored were reached under the old lists, and leaving them would make an edited config look like it did nothing, which is exactly the failure D73 is named after. And `0` has to leave the markers working, or the only way to stop guessing by repetition would be to stop classifying at all. | `an_opening_repeated_across_enough_chats_is_routine`, `an_opening_repeated_below_the_threshold_is_left_alone`, `a_zero_threshold_turns_repetition_off_but_not_the_markers`, `one_chat_listed_twice_still_counts_once`, `repetition_is_counted_across_every_directory` |
| TP-DAILY-23 | The daily section leaves out chats whose label is on `ui.hidden_chat_labels`, and the dropping happens where the section ASKS for its chats — before the count that limits it to five — never where those chats are remembered. Three chats are drawn anyway: one no rule recognised, one whose label is not on the list, and one that is OPEN in a tab right now. | This is the reported request — "sol altta rutin çalıştırılıp kapanan agentlar gösterilmesin" — and every clause of it is a way the fix could go wrong instead. Filter the remembered set and the rows vanish from every surface reading it, which shipped once as the first TP-DAILY-13 and had to be reverted when it took API-created workspaces off the client frame; `hiding_a_daily_chat_leaves_the_shared_set_alone` is the guard that stops it landing twice. Filter after the count and the section spends its five slots on chats it then declines to draw, so hiding chores makes the list SHORTER rather than cleaner — the person asked for the chores to move aside, not for their real chats to leave with them. Hide a chat that is running and the row that was the only way back to that tab is gone, which is losing work rather than tidying it. Hide an unlabelled chat and the feature starts guessing against the person. And an empty hidden list has to bring everything back intact, because the request was to show these somewhere else later, not to lose them. | `a_routine_chat_is_left_out_of_the_daily_section`, `an_empty_hidden_list_draws_every_chat_again`, `a_routine_chat_that_is_running_is_still_drawn`, `an_unlabelled_chat_is_always_drawn`, `hiding_a_daily_chat_leaves_the_shared_set_alone`, `hidden_chats_do_not_spend_the_sections_five_slots`, `a_section_whose_chats_are_all_hidden_has_no_chats` |
| TP-DAILY-24 | A chat's own menu carries **Label as...**, which opens a picker offering Context, Project, Routine, and — only on a chat that already wears one — Clear label. BOTH context-menu bodies open that picker and BOTH record the decision into `state.request_chat_label`; the App loop is the only thing that writes the ledger, where labels live in an additive `labels` map keyed by session id. A hand-written label outranks every rule, survives every resync, and is projected back LAST, after the merge that reclassifies everything. | A rule that cannot be overruled is a rule that eventually hides the wrong conversation, and the person watching it disappear has no way to say so. The precedence has to hold across a resync in particular: the derived verdicts are cleared and rebuilt on every sync, so a decision stored in that map would be erased by the next refresh — the reader labels a chat, the row comes back, and it vanishes again with nothing to explain why. Projecting the labels any earlier in the sync is the same failure that made `moves` and `names` apply last. Two menu bodies is the shape of #91: the `#[cfg(test)]` body is handed an `AppState` and cannot reach the ledger, so a verb written only there is green in the suite and dead in the product, which this file has already shipped twice. The additive `labels` map is the data-loss guard: a non-additive read makes a ledger written by yesterday's binary unreadable, and that ledger is the only record of which chats a person has been in — losing a label is an annoyance, losing the ledger is losing the history. And "Clear label" has to ask for a withdrawal rather than parse as a fourth label, or clearing would leave the chat wearing whatever that string happened to resolve to. | `a_hand_written_label_outranks_the_rules`, `a_sync_never_takes_back_a_hand_written_label`, `withdrawing_a_label_returns_the_chat_to_the_rules`, `labels_round_trip_and_an_old_file_loads_without_them`, `set_and_clear_label_report_change_honestly`, `the_chat_menu_offers_labelling_on_any_chat`, `both_context_menu_bodies_carry_the_labelling_verb`, `the_clear_item_withdraws_instead_of_labelling` |
| TP-DAILY-25 | The graveyard half of the agents panel asks the SAME question the daily section asks: a chat whose label is on `ui.hidden_chat_labels` gets no derived headstone. The gate is on DERIVING only — a death the graveyard actually witnessed stays in the store and on the screen whatever the rules make of that conversation. The seeding path passes the real verdict, not a stand-in. | This is the half of the reported request the daily filter could not reach, and it is the user's own wording: "rutin çalıştırılıp **kapanan agentlar** gösterilmesin". Measured on an isolated run: three chores taken out of the daily section walked straight back onto the screen underneath it, because `seed_closed_agents` derives ghosts from the very rows that section reads (`app/mod.rs`), so the chore that stopped being a chat became "an agent that closed". Gating the derivation rather than the store is the load-bearing distinction: a witnessed death is the only evidence that an agent ran at all, and hiding it would be losing history rather than tidying a list — the graveyard exists precisely to answer for the month before it started witnessing. The call site is pinned separately because a mutation proved it had to be: replacing the predicate there with "hide nothing" left every other test green, which is the shape of #91 in a different file — the piece works and the product does not. | `a_routine_conversation_gets_no_headstone`, `a_chat_nothing_hides_still_gets_its_headstone`, `a_witnessed_death_survives_the_hidden_filter`, `the_graveyard_and_the_daily_section_hide_the_same_chats`, `seeding_the_graveyard_asks_whether_the_chat_is_hidden` |
| TP-DAILY-26 | The panel DRAWS only the headstones a person should see: a record whose chat is hidden is skipped by all three of the panel's ghost passes — the count, the layouts and the records — while the store keeps it. The shipped markers include the code-review pass's own opening sentence, which announces itself in English rather than in a tag. | The first pass at this exempted witnessed deaths on the reasoning that a real agent's death must never be hidden. Measured on the reported machine, that reasoning was half right and the half that was wrong was the visible half: of 78 witnessed records, **38** were the code-review pass — an automation closing, not an agent someone was working with. So the exemption has to be about the STORE and not about the DRAWING, or the user is looking at exactly the rows they asked to stop seeing. Filtering two of the three passes instead of all three is worse than filtering none: `AgentPanelRow::Ghost(i)` indexes into both other lists, so a mismatch binds a row to someone else's headstone and a click revives the wrong conversation. And the marker has to ship by default because that sentence is written by a tool, never by a person — the same test that pins it also stops the fixture from drifting away from the real list, which is how this opening went unnoticed for a whole session. | `a_closed_chore_is_not_drawn_but_is_still_remembered`, `the_panels_three_ghost_passes_see_the_same_ghosts`, `routine_chat_keys_parse_and_default` |
