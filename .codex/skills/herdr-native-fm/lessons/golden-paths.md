# Herdr Native-FM Golden Paths

| Scenario | Steps | Precondition | Date |
|----------|-------|--------------|------|
| Audit noisy Codex startup without exposing secrets | Load continuity and common lessons; map warnings to agent, hook, plugin, and MCP declarations; parse only selected safe fields; check named listeners; run `agent-parity-status`; classify baseline-required versus profile-only capabilities | Repository handoff and user-level Codex config are readable | 2026-07-14 |
| Send a native-FM handoff file to the user's Mac over Tailscale | Verify source metadata and SHA-256; identify the single online macOS peer from structured Tailscale status; confirm it is a Taildrop target; ping it; run `tailscale file cp --verbose --update-interval=0 <file> <peer>:`; require the explicit `sent` result | Exactly one intended online Mac peer is identifiable | 2026-07-14 |
| Restore the shared MCP proxy after server-count growth breaks readiness | Verify cached MCP calls fail and 9100 has no listener; inspect only allowlisted service metadata; start the inactive knowledge backend without restarting live processes; measure the same config on an isolated port; raise bounded readiness from 30/45 seconds to 120/150 seconds; daemon-reload and start; require exact configured-set equality, critical initialize/tools, a real built-in MCP call, and full graph reindex | The proxy is failed/inactive, the config has 26 enabled servers, and isolated cold-start proof exceeds the old timeout | 2026-07-14 |
| Prove native-FM image pixels and cleanup in an isolated Kitty host | Create unique throwaway XDG config/state with Kitty graphics enabled; clear all Herdr socket/session identity variables; launch a uniquely titled X11 Kitty with a private remote-control socket; create/focus a throwaway workspace at the image directory; open FM and select the image with semantic keys; capture only the X11 window; crop the exact preview placement; resize the source to the captured placement and require image-compare equality; close FM and require the same crop to become one background color; exit with `prefix+q`; require zero test process/socket/root residue | The debug build is fully gated, a real source image exists, Kitty is installed, and stable Herdr/socket are never addressed | 2026-07-14 |
| Recover graph indexing after the proxy-owned codebase-memory child crashes in parallel extraction | Confirm the proxy remains active and logs show the exact child core dump; stop MCP retries; inspect the supported CLI and `CBM_WORKERS` capability; run one bounded `CBM_WORKERS=1 codebase-memory-mcp cli index_repository` process; require zero extraction errors; verify new node/edge counts and current production/test symbols with CLI status, search, and snippet calls | The proxy must not be restarted or killed, the graph store is still readable, and the crash stack identifies parallel native extraction | 2026-07-14 |
| Stabilize a lifecycle fixture that mixes wall time and synthetic deadlines | Identify the first causally inconsistent timestamp; replace every wrapper event in that scenario with its explicit `_at` seam on one strictly increasing clock; run the exact test, its lifecycle family, and a fresh full suite; keep the test-only fix in a separate commit | The production state machine already has deterministic time-injection seams and the failure appears only under parallel scheduling delay | 2026-07-15 |
| Land a bounded Miller production projection without changing visible output | RED on the missing `ViewState` consumer; project only from prepared `FmState`; carry typed Files generation, model revision, clamped window, and exact path/rect identities; clear close/foreign/degenerate targets; run named P1, cross-layer, full, platform, maintenance, Git, and graph gates | The pure bounded geometry core and typed Files Stage already exist, while render remains on a characterized compatibility path | 2026-07-17 |
| Validate Native Files through a real isolated server/client path without touching stable Herdr | Create unique XDG config/state/runtime roots and socket; start one test-owned foreground server; open a test-owned tmux shell; explicitly unset all Herdr identity, session, and socket variables before launching the debug client; exercise wide/narrow geometry, both dividers, outside capture, resize cancellation, wheel, permission, Unicode, preview fallback, close/reopen, and hidden-terminal input; stop through semantic commands; restore fixture permissions; require zero sockets, fds, root residue, and an unchanged stable socket inode/mode/mtime | The product/full gates are green, the debug binary is current, and only test-owned processes and paths are addressed | 2026-07-17 |
| Close a user-felt Native Files latency defect without speculative caching | Freeze the visible layout; graph the input-to-frame chain; capture lossless raw input plus stage timings in an isolated runtime; rank and falsify hypotheses; add behavior-specific REDs; reuse resident snapshots; move fallible reads to one bounded latest-pending worker; gate render on visible mutation; preserve lossless input/ordered control; run focused/full/cross-platform gates; obtain human live acceptance; record source, failure, cache, and cleanup lessons before publication | Stable Herdr/socket are excluded, the profiler uses bounded non-sensitive labels, and commit messages are aligned before exact-path staging | 2026-07-21 |
| Transfer a reference file-manager pattern into Herdr safely | Pin a clean source commit; cite exact source lines; separate observed mechanism from inferred benefit; diff it against current Herdr symbols through Codebase Memory; copy only invariants that fit Herdr ownership/bounds; explicitly reject incompatible policies such as an unbounded history map; freeze RED/failure/budget tests before Rust edits | The reference checkout identity is reproducible and every transfer claim carries tier plus confidence | 2026-07-21 |
| Correct vertical Miller navigation without speculative debounce or caching | Measure physical packets, decoded route, and reducer mutations separately; RED directory landing, explicit activation, stale/failure preview, and inert render; split cursor from activated chain; reuse the bounded latest worker; validate transient cursor authority; only then add the narrow measured wheel gate; run focused/full/platform/visual/live gates | Stable Herdr is excluded, raw telemetry is bounded/non-sensitive, and the terminal burst signature has exact owner/direction/coordinate/time evidence | 2026-07-21 |
| Audit resident-data versus focus authority after a Miller navigation refactor | Assert initial owner and resident depth separately; test vertical cursor-only movement; test explicit Right/Left; make render, auto-follow, hit geometry, resize projection, and watcher binding share `active_col()`; run adversarial 10,000-action invariants and full-frame narrow-layout tests | Prepared child snapshots may remain resident while the parent owns input | 2026-07-22 |
| Stabilize and approve Ratatui-to-Playwright visual fixtures without blind regeneration | Inject a fixed calendar anchor; equalize order-insensitive mtimes; use no-follow timestamp handling for links/FIFOs; wait for exact async identity/generation; export once; require generated JSON clean; inspect each changed PNG; update only the reviewed set; run all Chromium specs | The product composition change is intentional and the allowed snapshot scope is known before update | 2026-07-22 |
| Correct Miller horizontal keys without reopening vertical navigation | Freeze Left as one resident parent edge and Right/`l` as one directory-only child edge; graph the key-to-Trail route; RED file cursor mutation at reducer and App render layers; remove only the generic file-activation fallback; preserve bounded directory activation and Enter/click; run one-step, boundary, stale-worker, watcher, full, platform, visual, graph, and isolated physical gates | Vertical/wheel cursor semantics are already human-accepted and resident depth is separate from focus authority | 2026-07-22 |
