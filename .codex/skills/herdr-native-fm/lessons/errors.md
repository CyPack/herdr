# Herdr Native-FM Errors

| Error | Cause | Fix | Date |
|-------|-------|-----|------|
| codebase-memory MCP rejects `project="herdr"` with `project not found or not indexed` while the Herdr graph is healthy | Graph tools require the exact canonical project ID returned by `list_projects`, not a human-readable repository alias | Read `available_projects` or call `list_projects`, then use `project="home-ayaz-projects-herdr"` consistently for Herdr graph queries | 2026-07-17 |
| `codex mcp list` can expose credentials embedded in command arguments during an MCP audit | Codex masks values in the Env column but prints the Args column verbatim | Prefer structured TOML/plugin parsing that emits only server names, transports, enabled state, env key names, and path health; never surface raw `codex mcp list` output | 2026-07-14 |
| A narrow line-range read around an MCP section can still expose credentials | The requested range can spill into the adjacent `[mcp_servers.<name>.env]` table | Never use raw line windows for MCP config; parse TOML and emit a strict allowlist of safe fields | 2026-07-14 |
| `apply_patch` rejects a Herdr module edit with `Invalid Context` | The patch assumed a stale or reconstructed file header instead of the exact current worktree context | Inspect the narrow target region first, then reapply against the exact adjacent line; verify status to prove the rejected patch made no partial edit | 2026-07-14 |
| codebase-memory tools are listed but every call fails and port 9100 has no listener | The 26-server proxy cold start takes about 54 seconds, but readiness killed it after 45 seconds; cached tool schemas hid the dead transport | Without killing/restarting user processes, start a failed inactive backend if required, measure cold start on an isolated port, set readiness to a bounded 120 seconds inside a 150-second systemd budget, require exact 26/26 status plus real initialize/tools, then run the built-in MCP call and full reindex | 2026-07-14 |
| ShellCheck SC2016 flags the launcher prompt containing `$herdr-native-fm` | The skill trigger is intentionally literal inside single quotes and must not be expanded as a shell variable | Keep single quotes, add an adjacent explanation and a narrowly scoped `shellcheck disable=SC2016`, then rerun ShellCheck | 2026-07-14 |
| A multi-command verification shell continued to stage and commit after `cargo fmt --check` failed | The shell invocation did not enable fail-fast behavior, so newline-separated commands continued after a nonzero exit | Start multi-step gate/stage/commit commands with `set -euo pipefail`; immediately format, recheck, targeted-stage, and amend the unpublished commit before production work continues | 2026-07-14 |
| Linux `cargo clippy --all-targets -- -D warnings` rejects the bounded worker loop with `clippy::while-let-loop` | The worker used `loop` plus `let Some(request) = ... else { break; }`, which is correct but violates the repository's warning-as-error gate | Use the behavior-equivalent `while let Some(request) = take_next_request(...)` form, rerun the full B1 target set, then rerun Linux clippy before committing | 2026-07-14 |
| A scratch Herdr checkout pointed at the canonical repo target leaves link search paths into a deleted `/tmp/.../vendor/libghostty-vt/zig-out/lib` | Herdr's vendored build script records checkout-absolute paths in Cargo build output, so sharing target metadata across checkouts is unsafe | Give every scratch checkout its own target; if contaminated, confirm the stale path in `target/debug/build/herdr-*/output` and run `cargo clean -p herdr` before rebuilding the canonical checkout | 2026-07-14 |
| `cargo nextest list --message-format terse` is rejected before listing tests | This installed nextest supports `auto`, `human`, `oneline`, `json`, and `json-pretty`, not `terse` | Use `--message-format oneline`, inspect the selected inventory, and never count the rejected command as test evidence | 2026-07-14 |
| Kitty rejects `--allow-remote-control` while preparing an isolated host test | Kitty 0.46 exposes remote control as a config override rather than that CLI flag | Launch the throwaway host with `-o allow_remote_control=yes --listen-on unix:<throwaway-socket>`; use a unique X11 class/title and only semantic targeted input | 2026-07-14 |
| `cargo nextest list --ignored` is rejected as an unexpected argument | This installed nextest exposes ignored selection through `--run-ignored`, not a standalone `--ignored` flag | Use `cargo nextest list --locked --run-ignored only -E 'test(<name>)'` and never run a real-host ignored probe outside its isolated host contract | 2026-07-14 |
| `index_repository(mode="fast")` crashes the codebase-memory child with `munmap_chunk(): invalid pointer` and later MCP calls return an empty error | Native parallel Tree-sitter extraction corrupts memory while deleting parser/tree state; `mcp-proxy.service` remains active but the codebase-memory child core-dumps | Stop MCP retries and do not restart/kill the proxy; run the supported one-shot CLI with `CBM_WORKERS=1 codebase-memory-mcp cli index_repository '<json>'`, then prove freshness with CLI `index_status`, `search_graph`, and `get_code_snippet` against the same graph store | 2026-07-14 |
| A plugin manifest fixture fails with `invalid_plugin_min_herdr_version` before the action behavior under test is reached | Herdr requires every linked plugin manifest to declare a non-empty compatible `min_herdr_version`, even in focused unit fixtures | Include the current compatible baseline `min_herdr_version = "0.6.10"` in every valid plugin fixture; reserve omission or invalid values only for tests that explicitly target version validation | 2026-07-15 |
| A filesystem-preservation assertion expects the fixture filename bytes but reads `[120]` | The shared `TempDir::file(name)` helper writes the fixed payload `b"x"`; the test assumed it wrote `name` without inspecting the helper | Read the fixture helper before asserting payload bytes, or write the exact test payload explicitly; keep identity and content-preservation assertions separate | 2026-07-15 |
| A shell-based `rg` audit prints `command not found` for text inside backticks | Backticks inside a double-quoted shell command are command substitutions, even when intended as a literal Markdown search pattern | Put literal backtick-bearing regexes in single quotes or pass them as non-shell arguments; verify the failed read-only audit made no repository change before retrying | 2026-07-15 |
| `just check` fails with `just: command not found` on the current Herdr host | The repository prefers `just`, but the host does not have the `just` binary installed | Run the current recipe-equivalent gates directly with the required Zig path: fmt-check, full nextest with `--no-fail-fast`, maintenance tests, and the applicable clippy/visual gates; report the missing wrapper separately | 2026-07-19 |
| A focused delete test expects a request but the controller emits none | Direct controller fixtures do not inherit cursor-only authority; destructive action authority requires an explicit current multi-selection, even for one path | Call `replace_selection(0)` in the fixture before requesting Delete, then assert the exact ordered path authority and keep cursor focus separate from selection authority | 2026-07-15 |
| A compound Nextest filter is rejected before running tests | Multiple nested filter terms were combined with unbalanced parentheses | Prefer one balanced regex expression such as `test(/(delete|file_operation)/)`; run `cargo nextest list -E` first when building a new complex filter and never count a parse failure as test evidence | 2026-07-15 |
| A focused Nextest command with `test(=short_name)` exits 4 after selecting zero tests | Exact matching applies to the fully qualified test path, not only the short function name | Use one bounded regex such as `test(/(short_name_one|short_name_two)/)` or first list the fully qualified names; never count the empty selection as test or RED evidence | 2026-07-16 |
| A new composite Rust identity fails to derive `Hash` because its axis type is not hashable | `Hash` was added speculatively even though the current reducer does not use a hash collection or hashed key | Remove the unnecessary derive; add hashing to the complete nested identity only when a test-backed consumer requires it | 2026-07-16 |
| Rust E0524 rejects `Option::map_or_else` when both closures mutate the same state field | Both closures are constructed for one call and therefore require overlapping unique borrows even though only one branch executes | Compute the optional discriminator first, then use an explicit `if let` or `match` so each mutable field borrow has a disjoint lexical scope | 2026-07-16 |
| `Justfile` lookup fails even though the repository has Just recipes | The canonical recipe file is lowercase `justfile` on this checkout and the filesystem is case-sensitive | Inspect `justfile`; when `just` is absent, execute every command in the applicable recipe directly and record each gate separately | 2026-07-15 |
| A wrapped `exec_command` cell reports completion while its returned nested command still has a live `session_id` | The orchestration cell completed after yielding the nested PTY handle; it did not prove the Cargo process exited | Treat any returned `session_id` as authoritative, poll it with `write_stdin` until an explicit `exit_code`, and never launch another Cargo process while that handle is live | 2026-07-15 |
| `cargo nextest run <ignored-test>` exits 4 with `no tests to run` even though the full suite correctly reports the test skipped | Nextest filters out `#[ignore]` tests by default and treats the resulting empty run as an error | To prove the safety skip without executing the interactive test, run `cargo test <name> -- --nocapture` without `--ignored` and require `1 ignored, 0 failed`; use `cargo nextest list --run-ignored only` only for inventory | 2026-07-15 |
| A lifecycle test passes alone but intermittently misses fresh-process state under full-suite parallel load | The fixture mixes real `Instant::now()` wrappers with synthetic millisecond offsets, so a delayed real event can be newer than later synthetic evidence | Drive every event in the fixture through explicit `_at` seams using one monotonic synthetic clock, then require the exact test, related lifecycle family, and a fresh full suite | 2026-07-15 |
| A worker completion test expects the requested tag as the published filename | The shared `TempDir::copy_plan(name)` helper creates `source-{name}.txt`, so the destination preserves that complete source filename | Inspect helper path construction before asserting output identity; use `source-{name}.txt` and keep payload, path, and terminal-status assertions separate | 2026-07-15 |
| Linux all-target Clippy rejects a test fixture with `field_reassign_with_default` | The fixture creates `FileOperationWorkerState::default()` and then assigns several fields even though the repository treats every Clippy warning as an error | Build the complete fixture with one struct initializer plus `..Default::default()`, then rerun the exact recovery test and the full Linux Clippy gate before committing | 2026-07-15 |
| A split-lifecycle success test rolls back before its pane assertions when launched with `/usr/bin/true` | The fixture process exits immediately, closes the PTY input lane, and correctly drives the product's first-send rollback instead of the intended success branch | Use a long-lived test-owned argv process such as `/bin/cat` (or `more.com` on Windows), drive early exit deterministically with `PaneDied`, and shut down only the runtime created by the test | 2026-07-14 |
| An App-internal scheduled adapter is private when called from a sibling App module | `pub(super)` exposes the method only to its immediate parent module, while the scheduler lives under a different `crate::app` subtree | Use `pub(in crate::app)` for intentionally App-internal sibling-module seams, then require Linux all-target and canonical Windows Clippy before publication | 2026-07-15 |
| A full nextest run fails only the image preview worker target-size assertion after adding a fixed FM status row | The worker fixture duplicated the old preview-row geometry while production correctly consumed the new shared FM content rect | Update requested image target dimensions from the shared status-aware geometry, then require the exact worker test, broad image/Kitty/FM family, and a fresh full suite | 2026-07-15 |
| An isolated `tab list` smoke returns `ENOENT` in a pristine throwaway XDG root | Socket API commands are clients and do not implicitly start a headless server | Start one test-owned foreground `herdr server`, wait for its throwaway socket, run status/API checks, stop it through the same API, wait for normal exit, and require zero residue | 2026-07-15 |
| A detached tmux PTY readiness loop cannot capture a healthy Herdr pane by `session:0.0` | Tmux window and pane indexes may start at 1 even though the stable pane ID is `%0` | Query `list-panes`, target the returned `%<id>` for capture and semantic input, and close with Herdr keys instead of killing the tmux server | 2026-07-15 |
| A UI test using `TerminalRuntime::test_with_screen_bytes` panics with `there is no reactor running` before reaching its assertion | The Herdr test constructor creates a Tokio task and therefore requires a reactor even when the test only renders a cached screen | Follow existing runtime-render fixtures: use `#[tokio::test] async fn`, then rerun the exact test and reject the reactor panic as RED evidence | 2026-07-15 |
| A Files render characterization cannot find its full fixture filename even though the rendered surface visibly contains the file | Responsive Miller columns truncate entry labels by display cells, so a long identity marker is rendered with an ellipsis | Use a short unambiguous marker that fits the narrowest characterized column, include the bounded surface in failure output, and guard the temp root with `Drop` so assertion failures leave no residue | 2026-07-15 |
| A responsive-degradation mode table disagrees with rect assertions for the same layout | The mode helper resolves ContentBounded tracks with a zero-value closure while the rect assertions use non-zero component measurements, so the two assertions exercise different inputs | Pass the same measurement resolver to geometry and degradation helpers; classify the mismatched-fixture failure as diagnostic rather than product RED, then rerun the corrected exact test | 2026-07-15 |
| A production Miller projection test differs from pure geometry only by `Rect.y` and height | The test passed the whole Files Stage to the oracle while production correctly removed the one-row header and one-row status | Derive the expected body from the shared header/body/status frame contract, then keep path, focus, and `first_visible` assertions independent | 2026-07-17 |
| Linux all-target Clippy rejects new `FmState::new(&current_dir_pathbuf)` test fixtures with `needless_borrows_for_generic_args` | `FmState::new` accepts the owned generic path directly, so borrowing the temporary `PathBuf` is redundant under `-D warnings` | Pass the owned `PathBuf`, rerun the exact P1 tests, then rerun Linux and Windows Clippy before commit | 2026-07-17 |
| One-shot `index_repository` rejects a valid checkout with `repo_path is required` | The codebase-memory CLI schema names the repository argument `repo_path`; generic `path` and project-only arguments are not accepted | Use `CBM_WORKERS=1 codebase-memory-mcp cli index_repository '{"repo_path":"<absolute-checkout>","project":"<project-id>","incremental":true}'`, then prove freshness by finding a newly added symbol through the long-lived MCP channel | 2026-07-17 |
| Rust rejects `#[derive(Default)]` for a fixed profiler histogram larger than 32 elements | This toolchain does not provide `Default` for `[T; N]` at that array length even though array repetition initialization is supported | Implement `Default` explicitly and initialize the histogram with `[0; N]`; rerun the exact telemetry tests and all-target Clippy | 2026-07-17 |
| A recursive isolated-runtime residue scan fails even though only the intentional permission-denied fixture exists | `find` descends into a mode-000 directory under `set -euo pipefail`, so the audit itself fails before cleanup | Restrict socket/fd discovery to the known XDG config/runtime locations, restore fixture permissions before the guarded depth-first cleanup, and require the throwaway root to be absent afterward | 2026-07-17 |
| An isolated cleanup command reports a shell arithmetic syntax error around the file-descriptor count | `$(( ... ))` was used where command substitution was intended, so the shell parsed the `find` pipeline as arithmetic | Use `open_fds=$( (find ... || true) | wc -l )`, print the exact count, and do not treat the failed cleanup probe as resource evidence | 2026-07-17 |
| A test-owned tmux client immediately exits or reports `nested herdr disabled` despite unique socket overrides | The client inherited `HERDR_ENV`, workspace, tab, and pane identity variables; clearing only socket variables does not remove nested-session authority | Start a test-owned tmux shell, send an explicit command that unsets every Herdr identity/session/socket variable, then detach Herdr semantically and exit only that test shell | 2026-07-17 |
| The `rust` launcher demands `Skill(rust-dev)` but every rust-dev reference/lesson read fails | `~/.codex/skills/rust-dev` is a symlink to `~/.claude/skills/rust-dev`, which does not exist on this machine | Do not pretend the skill was loaded; use the herdr-local catalog (`docs/patterns/rust-engineering.md` HP1-HP10 + `docs/references/README.md`), record the broken symlink as a pre-Rust blocker, and repair or re-point the symlink before implementation | 2026-07-18 |
| Continuity records "55 unique `TP-FIP-*`" while a deterministic `grep -o 'TP-FIP-[A-Z0-9]*-[0-9]*' \| sort -u` over the design returns 57 | The recorded figure excluded the two `TP-FIP-E2E-*` IDs; both counts describe the same design | Treat the deterministic 57 as coverage authority (map all 57, drop none), state the counting difference explicitly in plan/continuity, and do not stop on it as inventory drift | 2026-07-18 |
