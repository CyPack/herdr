# Ratatui Intelligence Agent Contract

This subtree is the research and adaptation module at version `2.1.0`.

- Use Codebase Memory before filesystem discovery for graph-dependent claims.
  If MCP evidence is unavailable, mark the phase `blocked`; do not fabricate a
  replacement. Preserve `partial` when evidence remains unresolved.
- Keep product code and the stable runtime outside this module. Research and
  planning do not authorize implementation or runtime operations.
- Use TDD for schemas, validators, templates, pipeline changes, and evals:
  commit a valid RED contract before its GREEN implementation.
- Preserve P0-P14 IDs, legacy statuses, and legacy artifacts additively.
- Stage only exact module paths with targeted staging. Never absorb unrelated
  worktree changes.
- The P14 output targets the sibling `herdr-change-pipeline`; handoff does not
  grant product authority.
- CyPack push and shared-checkout fast-forward are publication gates, not
  automatic consequences of local validation.
