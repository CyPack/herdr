# Errors

| error | cause | fix | date |
|---|---|---|---|
| `get_code_snippet` reports project not found even though the qualified name contains a project prefix | Codebase Memory CLI requires a separate `project` argument | Pass both exact `project` from `list_projects` and `qualified_name`; do not infer transport failure | 2026-07-15 |
| Built-in Codebase MCP call returns an empty `isError` result | Current MCP transport is unavailable even though the graph store and CLI are healthy | Stop MCP-backed claims; use supported single-worker CLI against the same store and label evidence `CLI-backed Codebase Memory` | 2026-07-15 |
| Parallel indexing crashes with native allocator output such as `munmap_chunk()` | Tree-sitter/native extraction is unsafe under this environment's concurrent worker path | Set `CBM_WORKERS=1`, index repositories sequentially, preserve the failure record and record the successful retry separately | 2026-07-15 |
| A web authoring tool appears to provide pixel-rounded Ratatui controls | CSS preview semantics were mistaken for terminal output semantics | Inspect the Ratatui exporter; map rounded styling to `BorderType::Rounded` and describe it as Unicode glyph illusion | 2026-07-15 |
| Eval coverage checker reports a missing term containing `_` | Shell loop transformed underscores or split multi-word terms before fixed-string search | Stream JSON terms with `while IFS= read -r term` and pass each unchanged to `rg -F` | 2026-07-15 |
| `python -m unittest .codex/.../test_file.py` raises `ValueError: Empty module name` | `unittest` treats the leading-dot filesystem path as a dotted module name | Execute the test file directly or use `python -m unittest discover -s <tests-dir> -p 'test_*.py'` | 2026-07-15 |
| A large `apply_patch` update fails with `Failed to find expected lines` | One broad hunk depended on exact surrounding prose and a small line-wrap/context mismatch invalidated the entire patch | Inspect the exact local sections and split the change into small independently anchored hunks | 2026-07-15 |
| Source-wide eval check flags a `must_not_include` phrase such as `pixel radius` | Negative eval phrases legitimately occur in guardrails that explicitly reject the behavior; `must_not_include` describes generated answers, not forbidden source vocabulary | Validate positive term coverage and JSON structure statically; enforce negative terms only against actual eval outputs with negation/context-aware assertions | 2026-07-15 |
| `validate_reference_pipeline.py --help` or `--skill-root ...` fails by trying to open the flag as a file | The validator has no `argparse` interface and accepts only positional `<pipeline.json> [run-template.json]` paths | Call the validator with the canonical pipeline path and optional run-template path; use its source-level usage string instead of assuming standard flags | 2026-07-15 |
| A fail-fast audit stops after piping `rg` into `head` even though matching data exists | `head` closes the pipe early and `set -o pipefail` treats `rg` receiving SIGPIPE as a command failure | Avoid `head` in fail-fast pipelines; use bounded search options, structured filters, or a non-early-closing reader | 2026-07-15 |
| `git add` refuses modified tracked design specs under `docs/superpowers` as ignored | The repository `/docs/*` rule still requires explicit force-add for these intentionally tracked local design files | Verify the exact paths with `git ls-files --stage`, then use `git add -f -- <exact-spec-paths>` and inspect the staged name list before committing | 2026-07-15 |
| Normal targeted `git add` stages 27 of 28 baseline files and omits `.cartography/SYSTEM-MAP.json` | The repository ignore rules match every `.cartography` directory even though this module requires the map as a frozen artifact | Compare the frozen inventory with the staged list, then force-add only the exact system-map path and recheck the boundary | 2026-07-15 |
| Baseline `git diff --cached --check` reports new blank lines at EOF in four reference documents | User-authored baseline Markdown contained an extra terminal blank line that Git rejects as whitespace damage | Inspect exact file tails, remove only the final blank line, recompute the baseline hash, and rerun the baseline tests before commit | 2026-07-15 |
